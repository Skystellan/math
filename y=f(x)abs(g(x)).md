# 关于 $y=f(x)|g(x)|$ 可导性的探讨
在这里，我们首先要明确 $f(x)$ 和 $g(x)$ 这两个函数具有怎样的特性。既然是探讨两个函数乘积的可导性，我们自然可以要求原函数可导。因此，以下的讨论均建立在 $f(x), g(x)$ 在各自定义域内连续且可导的条件下。

讨论问题，需要有个切入点，很自然能想到，$f(x)|g(x)|$ 的可导性，应该主要是受 $|g(x)|$ 的可导性的限制，因此首先，我们先来探讨 $|g(x)|$ 的可导性

# $|g(x)|$ 的可导性

那么绝对值符号，会使得原函数什么地方变得不可导呢？首先毋庸置疑，在原函数中，函数值为正的区域，其可导性不变，依然处处可导，因为绝对值符号对这一段区域毫无影响。而对于非正区域呢？自然想到把零点和负值点区分讨论。

我们首先讨论负值点的区域。由几何直观结合对称性，很容易看出来，对于负值区域，通过绝对值翻转到 x 轴上方后，原来可导的地方依然可导，只是符号发生的变化。以下是两个常用的函数图像：
![](https://cdn.mathpix.com/cropped/2025_06_15_cdb324313e22a752bb47g-1.jpg?height=549&width=1466&top_left_y=1408&top_left_x=301)
![](https://cdn.mathpix.com/cropped/2025_06_15_cdb324313e22a752bb47g-1.jpg?height=538&width=1446&top_left_y=1988&top_left_x=308)
（a）$y=\ln x$
![](https://cdn.mathpix.com/cropped/2025_06_15_cdb324313e22a752bb47g-2.jpg?height=567&width=1475&top_left_y=239&top_left_x=299)
![](https://cdn.mathpix.com/cropped/2025_06_15_cdb324313e22a752bb47g-2.jpg?height=570&width=1475&top_left_y=806&top_left_x=299)
（b）$y=\sin x$
接下来我们进行证明：
对于 $|g(x)|$ 在 $x=x_{0}$ 处的可导性，其中 $g\left(x_{0}\right)<0$ 。
由极限的保号性，在 $x_{0}$ 的某个领域内，均有 $g(x)<0$ ，因此
有
$$
\lim _{x \rightarrow x_{0}} \frac{|g(x)|-\left|g\left(x_{0}\right)\right|}{x-x_{0}}=\lim _{x \rightarrow x_{0}} \frac{-\left[g(x)-g\left(x_{0}\right)\right]}{x-x_{0}}=-g^{\prime}\left(x_{0}\right)
$$

因此 $|g(x)|$ 在 $x=x_{0}$ 处可导。
而对于 $g(x)$ 的零点呢？我们也可以做同样的分析。对于函数的零点，只有两种情况，要么该点导数为 0 ，要么该点导数不为 0 。当该点导数为 0 时，很容易证明在加上绝对值后，该点的左导数与右导数相等，即可导。而当该点导数不为 0 的时候，就要进行分类了。由上面对于负值区域的导数符号变化分析易知，当 $g(x)<0$ 的时候， $|g(x)|$ 导数的符号与 $g(x)$ 相反。而如果该点导数不为 0 ，则说明其两边异号（由保号性很容易证明），负号的那边在加绝对值后，对应端导数（左导数或者右导数）会变号，而正号端对应导数不变，导致左右导数不相等，最终该点不可导。

因此我们有如下结论，$|g(x)|$ 在 $x=x_{0}$ 处不可导，当且仅当 $g\left(x_{0}\right)=0$ 且 $g^{\prime}\left(x_{0}\right) \neq$ 0 。其他的自变量取值，$|g(x)|$ 均可导。

事实上，结合函数图像，我们很容易发现不可导点往往都具有＂尖点＂的几何特征，也就是不光滑，对于尖点，其左右的切线会有明显的变化，也就是左右导数不相等。

# $y=f(x)|g(x)|$ 可导性的探讨

对于 $|g(x)|$ 的可导点（当然，要在 y 的定义域内），一定是 y 的可导点（我们不妨令 $m(x)=|g(x)|$ ，此时 $y=f(x) m(x)$ ，又 $f, m$ 在 $x=x_{0}$ 处可导，故 $y$ 在 $x=x_{0}$ 处可导），因此接下来，我们仅需要讨论 $|g(x)|$ 的不可导点。也就是 $g(x)$ 的导数不为 0 的零点。

不妨设该点为 $x_{0}$ 的左边大于 0 ，右边小于 0 （其中一个为 0 也可以，不影响讨论结果），也就是函数图像类似于 $\ln x$ 的走势。

则该函数在零点附近，可以写成分段形式：
$$
y=\left\{\begin{array}{c}
f(x) g(x), x<x_{0} \\
-f(x) g(x), x>x_{0}
\end{array}\right.
$$

则有
$$
\begin{gathered}
\lim _{x \rightarrow x_{0}^{-}} \frac{f(x) g(x)-f\left(x_{0}\right) g\left(x_{0}\right)}{x-x_{0}}=\lim _{x \rightarrow x_{0}^{-}} \frac{f(x) g(x)}{x-x_{0}}=f\left(x_{0}\right) \lim _{x \rightarrow x_{0}^{-}} \frac{g(x)-g\left(x_{0}\right)}{x-x_{0}} \\
=f\left(x_{0}\right) g_{-}^{\prime}\left(x_{0}\right)=f\left(x_{0}\right) g^{\prime}\left(x_{0}\right)
\end{gathered}
$$
（补充说明：能这样运算的原因在于，$f(x)$ 在 $\left(x_{0}\right)$ 处连续，且 $g^{\prime}\left(x_{0}\right)$ 存在）
即 $y_{-}^{\prime}\left(x_{0}\right)=f\left(x_{0}\right) g^{\prime}\left(x_{0}\right)$
同理，$y_{+}^{\prime}\left(x_{0}\right)=-f\left(x_{0}\right) g^{\prime}\left(x_{0}\right)$
而函数在某点可导的充要条件是该点左导数右导数存在且相等，在该情况下，左右导数均存在，只需考虑相等，即
$$
f\left(x_{0}\right) g^{\prime}\left(x_{0}\right)=-f\left(x_{0}\right) g^{\prime}\left(x_{0}\right)
$$

得
$$
f\left(x_{0}\right) g^{\prime}\left(x_{0}\right)=0
$$

因此，很容易就能得到，$y=f(x)|g(x)|$ 在 $x=x_{0}$ 处可导的充要条件为 $f\left(x_{0}\right)=0$ 或 $g^{\prime}\left(x_{0}\right)=0$（这里事实上已经把 $g(x)$ 的可导点包括在内）。

总结来看，$y=f\left(x_{0}\right)\left|g\left(x_{0}\right)\right|$ 有如下分类情况：

（1）$x_{0}$ 不是 $g(x)$ 的零点，此时 $y$ 可导
（2）$x_{0}$ 是 $g(x)$ 的零点，那么 y 可导当且仅当 $f\left(x_{0}\right)=0$ 或 $g^{\prime}\left(x_{0}\right)=0$

**<span style="font-size: 1.25em;">重申一下前提条件：$f(x)$ 与 $g(x)$ 均在 $x_{0}$ 处可导**

那么如何从几何理解情况（2）呢。这里我们需要看见 $f(x)$ 的取值会影响 $|g(x)|$ 的尖点情况。 $g^{\prime}\left(x_{0}\right)=0$ 不必多说，因为此时 $|g(x)|$ 在 $x=x_{0}$ 处可导。而当 $g^{\prime}\left(x_{0}\right) \neq 0$ 时， $|g(x)|$ 在 $x=x_{0}$ 处出现了尖点，这是唯一 $y$ 可能不可导的地方，而 $f(x)$ 在满足什么条件时，会使得这个尖点在 $y$ 中消失呢，通过上面的推导，容易发现在 $y$ 中，$f\left(x_{0}\right)$ 与 $g^{\prime}\left(x_{0}\right)$共同决定着该点的导数，也就是决定着该点的图像趋势，如果 $f\left(x_{0}\right) \neq 0$ ，那么把 $f\left(x_{0}\right)$作用到 $g^{\prime}\left(x_{0}\right)$ 上，只是使得其左右导数进行了等比例的放大或缩小，尖点依然存在，而当 $f\left(x_{0}\right)=0$ 时，直接将尖点抹平了，就好像 $f\left(x_{0}\right)$ 是一个坚硬的物体或平面，将此处的棱角直接磨光滑了。

而在考研中，常考的题型中，往往 $g(x)$ 是以多项式的形式存在的，那么我们可不可以根据其多项式的特征，得出进一步的结论呢？当然可以。

同样的，我们只需考虑 $g(x)$ 的零点，很显然，若假设 $g(x)$ 有零点 $x_{1}, x_{2}\left(x_{1}<x_{2}\right)$ ，那么 $g(x)$ 能够表示成如下形式：
$$
g(x)=n(x)\left(x-x_{1}\right)\left(x-x_{2}\right)
$$

其中，$n(x)$ 仍是多项式函数，且无零点（令 $1=x^{0}$ ，其也可以视为多项式函数）
零点当然可以更多，写法是一样的。
那么 $y=f(x)|g(x)|$ 可以写成如下形式：
$$
y=f(x) \ln (x)| | x-x_{1}| | x-x_{2} \mid
$$

而由于 $n(x)$ 无零点，$|n(x)|$ 一定可导，那么我们可以将其与 $f(x)$ 合并，不影响接下来的讨论令其为 $l(x)$ ，即
$$
y=l(x)\left|x-x_{1}\right|\left|x-x_{2}\right|
$$

由之前的分析，容易知道，$x_{1}, x_{2}$ 是可能的不可导点，而当 $x=x_{1}$ 时 $x-x_{2} \neq 0$ ，即 $\left|x-x_{2}\right|$ 在 $x=x_{1}$ 处可导，我们在分析 $y$ 在 $x=x_{1}$ 处的可导情况时，完全可以把 $\left|x-x_{2}\right|$与 $l(x)$ 合并，同理，分析 $y$ 在 $x=x_{2}$ 处的可导情况时可以把 $\left|x-x_{1}\right|$ 与 $l(x)$ 合并。接下来，我们着重分析 $y$ 在 $x=x_{1}$ 处的可导情况。

由以上分析，可知，仅需分析：
$$
y=\pi(x)\left|x-x_{1}\right|
$$

其中 $\pi(x)=l(x)\left|x-x_{2}\right|$ ，即 $\pi(x)$ 在 $x=x_{1}$ 处可导
那么要使 $y=\pi(x)\left|x-x_{1}\right|$ 在 $x=x_{1}$ 可导，需要什么条件呢，显然是 $\pi\left(x_{1}\right)=0$ ，为什么？还记得上面的结论吗？
（1）$x_{0}$ 不是 $g(x)$ 的零点，此时 $y$ 可导
（2）$x_{0}$ 是 $g(x)$ 的零点，那么 y 可导当且仅当 $f\left(x_{0}\right)=0$ 或 $g^{\prime}\left(x_{0}\right)=0$

这里再重申一遍前提条件，$f(x)$ 与 $g(x)$ 均在 $x_{0}$ 处可导。
而 $\pi(x)$ 在 $x=x_{1}$ 可导，$x-x_{1}$ 在 $x=x_{1}$ 的导数为 1 ，因此只能有 $\pi\left(x_{1}\right)=0$ ，而且其为充分必要条件。（希望这个分析过程，能够带你们领略数学抽象并富含逻辑的艺术——在得到某些结论时，抽象出其需要的条件；而在使用中，只要满足这些条件，便可直接应用结论。）对于 $x=x_{2}$ ，同样的，得有 $l(x)\left|x-x_{1}\right|$ 在 $x=x_{2}$ 处的函数值为 0 ，对于其他零点，结论同样。也就是说，我们能得到如下结论：

设 $f(x)=g(x)|x-a|$ ，其中 $g(x)$ 在 $x=a$ 处可导，则 $f(x)$ 在 $x=a$ 处可导的充要条件为 $g(a)=0$ 。

而如果 $x=a$ 是重根呢？比如 $f(x)=g(x)|x-a||x-a|$ 。我们可以有可以进行以下两种方式进行讨论：
（1）先讨论 $g(x)|x-a|$ 在 $x=a$ 处的可导性，再讨论 $g(x)|x-a||x-a|$ 在 $x=a$ 处的可导性。比如 $g(x)|x-a|$ 在 $x=a$ 处可导，最后问题就转化为了讨论 $m(x)|x-a|$ 在 $x=a$处的可导性，但如果 $g(x)|x-a|$ 在 $x=a$ 处不可导，那就要小心了，
$g(x)|x-a||x-a|$ 在 $x=a$ 处不一定不可导哦。
（2）直接判断 $\left|(x-a)^{2}\right|$ 在 $x=a$ 处可不可导，若可导，则 $g(x)|x-a||x-a|$ 在 $x=a$ 处可导，若不可导，那和 $x=a$ 为单根的时候分析是一样的。

这里埋下一个问题，这里 $a$ 是 k 重根，请问这个 k 对最后的可导性有怎样的影响呢？希望有同学能够自行分析一遍。我只把结论放这里：

### 假设 $g(x)$ 在 $x=a$ 处可导
**$k$ 如果是奇数，则 $x=a$ 为 $|g(x)|$ 的不可导点，此时 $f(x)=g(x)|x-a|^{k}$ 在 $x=a$ 处可不可导就要看 $g(a)$ 的取值。**
**$k$ 如果是偶数，则 $x=a$ 为 $|g(x)|$ 的可导点，则 $f(x)=g(x)|x-a|^{k}$ 在 $x=a$ 处可导。**



再看看这道 1998 年数一的真题
函数 $f(x)=\left(x^{2}-x-2\right)\left|x^{3}-x\right|$ 不可导点的个数为（ ）．
（A） 3
（B） 2
（C） 1
（D） 0

我们一样可以化为如下形式：
$$
f(x)=\left(x^{2}-x-2\right)|x||x-1||x+1|
$$

可能的不可导点为 $0,1,-1$ 分析同上，容易知道，当 $x=0$ 或 1 时，$f(x)$ 不可导。选 $\mathbf{B}$
最后，经过上述分析，可以想象到，在某些情况下，对于 $f(x)$ 的不可导点，$|g(x)|$能够使其原本不可导的点变得可导。有意愿的同学可以去尝试构造一个这样的函数。



**重根可导性证明**
设函数为 $F(x) = h(x) |x-a|^k$，其中我们假设 $h(x)$ 在 $x=a$ 处可导（根据你上下文的设定，你称其为 $g(x)$，为了避免混淆，我这里用 $h(x)$ 表示与 $|x-a|^k$ 相乘的那个函数）。

我们要讨论 $F(x)$ 在 $x=a$ 处的可导性。 首先，$F(a) = h(a)|a-a|^k = h(a) \cdot 0 = 0$ (假设 $k \ge 1$ 使该项为0)。

根据导数的定义，$F'(a)$ 存在当且仅当以下极限存在： $$ F'(a) = \lim_{x \to a} \frac{F(x) - F(a)}{x-a} = \lim_{x \to a} \frac{h(x)|x-a|^k - 0}{x-a} $$ 由于 $h(x)$ 在 $x=a$ 处可导，所以它在 $x=a$ 处连续，即 $\lim_{x \to a} h(x) = h(a)$。 因此， $$ F'(a) = h(a) \lim_{x \to a} \frac{|x-a|^k}{x-a} $$ 我们需要分析极限 $L = \lim_{x \to a} \frac{|x-a|^k}{x-a}$。为此，我们考虑左极限和右极限。

情况一：$k$ 是偶数 设 $k=2m$，其中 $m$ 是正整数 (如果 $k=0$，则 $|x-a|^0=1$，是可导的)。 当 $k$ 是偶数时，$|x-a|^k = (x-a)^k$。 所以， $$ L = \lim_{x \to a} \frac{(x-a)^k}{x-a} = \lim_{x \to a} (x-a)^{k-1} $$ 因为 $k$ 是偶数且 $k \ge 2$ (通常讨论的场景)，所以 $k-1 \ge 1$。 因此，$L = (a-a)^{k-1} = 0$。 所以，$F'(a) = h(a) \cdot 0 = 0$。 这意味着当 $k$ 是偶数时，$F(x) = h(x)|x-a|^k$ 在 $x=a$ 处总是可导的，且导数为0。

这也印证了你的第二条结论： “$k$ 如果是偶数，则 $x=a$ 为 $|(x-a)^k|$ 的可导点，则 $F(x)=h(x)|x-a|^{k}$ 在 $x=a$ 处可导。” 确实，当 $k$ 是偶数时，$|(x-a)^k| = (x-a)^k$。若 $k \ge 2$，则 $(x-a)^k$ 在 $x=a$ 处可导且导数为0。若 $k=0$，则 $|(x-a)^0|=1$，在 $x=a$ 处可导且导数为0。由于 $h(x)$ 在 $x=a$ 处可导，两个在 $x=a$ 处可导的函数之积仍在 $x=a$ 处可导。

情况二：$k$ 是奇数 设 $k=2m+1$，其中 $m$ 是非负整数 (即 $k \ge 1$)。 我们需要分别计算左极限 $L_-$ 和右极限 $L_+$。

右极限 ($x \to a^+$): 此时 $x > a$，所以 $x-a > 0$，因此 $|x-a| = x-a$。 $$ L_+ = \lim_{x \to a^+} \frac{(x-a)^k}{x-a} = \lim_{x \to a^+} (x-a)^{k-1} $$ 由于 $k$ 是奇数，$k-1$ 是偶数。 如果 $k=1$，则 $k-1=0$，所以 $L_+ = \lim_{x \to a^+} (x-a)^0 = 1$ (对于 $x \neq a$)。 如果 $k \ge 3$ (奇数)，则 $k-1 \ge 2$，所以 $L_+ = (a-a)^{k-1} = 0$。

左极限 ($x \to a^-$): 此时 $x < a$，所以 $x-a < 0$，因此 $|x-a| = -(x-a)$。 $$ L_- = \lim_{x \to a^-} \frac{(-(x-a))^k}{x-a} = \lim_{x \to a^-} \frac{(-1)^k (x-a)^k}{x-a} $$ 因为 $k$ 是奇数，所以 $(-1)^k = -1$。 $$ L_- = \lim_{x \to a^-} \frac{-(x-a)^k}{x-a} = \lim_{x \to a^-} -(x-a)^{k-1} $$ 如果 $k=1$，则 $k-1=0$，所以 $L_- = \lim_{x \to a^-} -(x-a)^0 = -1$ (对于 $x \neq a$)。 如果 $k \ge 3$ (奇数)，则 $k-1 \ge 2$，所以 $L_- = -(a-a)^{k-1} = 0$。

现在我们来分析 $F'(a)$ 的左右导数： $F'_+\left( a \right) =h\left( a \right) L_+,  F'_-\left( a \right) =h\left( a \right) L_-$

子情况 2.1：$k=1$ (奇数) $L_+=1$，$L_-=-1$。 $F'_+(a) = h(a) \cdot 1 = h(a)$。 $F'_-(a) = h(a) \cdot (-1) = -h(a)$。 $F(x)$ 在 $x=a$ 处可导的充要条件是 $F'_+(a) = F'_-(a)$，即 $h(a) = -h(a)$，这意味着 $2h(a)=0$，所以 $h(a)=0$。 此时，你的第一条结论中 "$x=a$ 为 $|(x-a)^k|$ 的不可导点" (对于 $k=1$，即 $|x-a|$ 在 $x=a$ 处不可导) 是正确的。并且 "$F(x)=h(x)|x-a|^{k}$ 在 $x=a$ 处可不可导就要看 $h(a)$ 的取值" 也是正确的 (当 $h(a)=0$ 时可导，否则不可导)。

子情况 2.2：$k \ge 3$ 且 $k$ 是奇数 $L_+=0$，$L_-=0$。 所以 $L=0$。 $F'_+(a) = h(a) \cdot 0 = 0$。 $F'_-(a) = h(a) \cdot 0 = 0$。 因此，$F'(a)=0$。这意味着当 $k \ge 3$ 且 $k$ 是奇数时，$F(x) = h(x)|x-a|^k$ 在 $x=a$ 处总是可导的，且导数为0，这与 $h(a)$ 的取值无关。

总结与修正：

假设 $h(x)$ 在 $x=a$ 处可导，令 $F(x) = h(x)|x-a|^k$。

如果 $k$ 是偶数 ($k \ge 0$)： $F(x)$ 在 $x=a$ 处可导。
如果 $k=1$ (奇数)： $F(x)$ 在 $x=a$ 处可导的充要条件是 $h(a)=0$。
如果 $k \ge 3$ 且 $k$ 是奇数： $F(x)$ 在 $x=a$ 处可导 (且 $F'(a)=0$)。