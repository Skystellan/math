这是判别一类重要反常积分（或称瑕积分）收敛性的“万能公式”，通常被称为贝特朗积分 (Bertrand's integral) 的判别法。

它之所以被称为“万能”或“标准型”，是因为它处理了由两种最常见的函数——幂函数 $x^\alpha$ 和 对数函数 $\ln^\beta x$——组合而成的被积函数。理解了它的原理，就能处理绝大多数复杂的收敛性判断问题。

其原理完全基于极限比较判别法和对函数在无穷大/无穷小处“增长速度”的深刻理解。

核心原理：函数的“阶”或“增长速度”
在判断收敛性时，我们关心的是当 $x$ 趋于瑕点（这里是 $0$ 和 $\infty$）时，被积函数 $f(x)$ 趋于无穷的速度。我们通常将其与一个我们熟知的、更简单的函数 $g(x)$（即p-积分）进行比较。

p-积分判别法 (标准型)：

$\int_a^\infty \frac{1}{x^p} dx$：当 $p>1$ 时收敛，当 $p \le 1$ 时发散。
$\int_0^b \frac{1}{x^p} dx$：当 $p<1$ 时收敛，当 $p \ge 1$ 时发散。
函数增长速度的层次：当 $x \to \infty$ 时，对数函数增长得比任何正次幂的幂函数都慢。 $$ \lim_{x\to\infty} \frac{\ln^\beta x}{x^\epsilon} = 0 \quad (\text{对于任意 } \beta>0, \epsilon>0) $$

详细分析
要判断 $\int_0^\infty \frac{1}{x^\alpha \ln^\beta x} dx$ 的收敛性，必须将其拆分为两部分，例如在 $x=2$ 处拆开（任何在(0, ∞)内的点都可以，但避开 $x=1$ 会让 $\ln x$ 不为零，更方便）： $$ \int_0^2 \frac{1}{x^\alpha \ln^\beta x} dx + \int_2^\infty \frac{1}{x^\alpha \ln^\beta x} dx $$ 原积分收敛，当且仅当这两部分积分都收敛。

1. 分析在 $x \to \infty$ 时的收敛性
我们分析 $\int_2^\infty \frac{1}{x^\alpha \ln^\beta x} dx$。

情况一：$\alpha > 1$ (幂函数占主导，使其收敛) 此时 $x^\alpha$ 增长得非常快，足以保证积分收敛。$\ln^\beta x$ 这一项虽然也在增长（如果 $\beta>0$），但它增长得太慢了，无法改变收敛的命运。 严格证明：我们取一个介于 1 和 $\alpha$ 之间的数，比如 $p = \frac{1+\alpha}{2}$。显然 $p>1$。 我们比较原函数和 $\frac{1}{x^p}$： $$ \lim_{x\to\infty} \frac{\frac{1}{x^\alpha \ln^\beta x}}{\frac{1}{x^p}} = \lim_{x\to\infty} \frac{x^p}{x^\alpha \ln^\beta x} = \lim_{x\to\infty} \frac{1}{x^{\alpha-p} \ln^\beta x} = 0 $$ 因为 $\int_2^\infty \frac{1}{x^p} dx$ 收敛（由于 $p>1$），且极限为0，根据极限比较判别法，原积分也收敛。这个结论对任意 $\beta$ 都成立。

情况二：$\alpha < 1$ (幂函数占主导，使其发散) 此时 $x^\alpha$ 增长得不够快，不足以使积分收敛。$\ln^\beta x$ 的影响不足以扭转局面。 严格证明：类似地，取 $p = \frac{1+\alpha}{2}$，此时 $p<1$。比较原函数和 $\frac{1}{x^p}$，会发现极限为0。但这无法判断。我们应该比较原函数和 $\frac{1}{x}$。 $$ \lim_{x\to\infty} \frac{\frac{1}{x^\alpha \ln^\beta x}}{\frac{1}{x}} = \lim_{x\to\infty} \frac{x}{x^\alpha \ln^\beta x} = \lim_{x\to\infty} \frac{x^{1-\alpha}}{\ln^\beta x} = \infty $$ 因为 $\int_2^\infty \frac{1}{x} dx$ 发散，且极限为 $\infty$，原积分也发散。

情况三：$\alpha = 1$ (幂函数在临界状态，对数函数成为决定因素) 此时被积函数为 $\frac{1}{x \ln^\beta x}$。这是最关键的情况。 我们可以用积分法直接判断。令 $u = \ln x$，则 $du = \frac{1}{x} dx$。 $$ \int_{\ln 2}^{\infty} \frac{1}{u^\beta} du $$ 这变成了一个标准的 p-积分！根据 p-积分判别法，它当且仅当 $\beta > 1$ 时收敛。

小结 $x \to \infty$：积分收敛的条件是 $\alpha > 1$ 或者 $\alpha=1, \beta>1$。这与图片中的结论完全一致。

2. 分析在 $x \to 0^+$ 时的收敛性
我们分析 $\int_0^2 \frac{1}{x^\alpha \ln^\beta x} dx$。 这个情况直接看比较麻烦，有一个绝妙的技巧：变量替换。 令 $t = 1/x$，则 $x = 1/t$，$dx = -\frac{1}{t^2} dt$。 当 $x \to 0^+$ 时，$t \to \infty$。当 $x=2$ 时，$t=1/2$。 $$ \int_{\infty}^{1/2} \frac{1}{(1/t)^\alpha \ln^\beta(1/t)} \left(-\frac{1}{t^2}\right) dt $$ 利用 $\ln(1/t) = -\ln t$ 和积分换序，上式变为： $$ \int_{1/2}^{\infty} \frac{t^\alpha}{(-\ln t)^\beta \cdot t^2} dt = \frac{1}{(-1)^\beta} \int_{1/2}^{\infty} \frac{1}{t^{2-\alpha} \ln^\beta t} dt $$ 常数因子 $\frac{1}{(-1)^\beta}$ 不影响收敛性。现在问题变成了判断 $\int_{1/2}^{\infty} \frac{1}{t^{2-\alpha} \ln^\beta t} dt$ 的收敛性，这是一个我们刚刚在第一部分解决的 $t \to \infty$ 的问题！

我们直接套用第一部分的结论，令新的幂次为 $p = 2-\alpha$： 积分收敛的条件是：

$p > 1 \implies 2-\alpha > 1 \implies \alpha < 1$
或者 $p=1, \beta>1 \implies 2-\alpha=1, \beta>1 \implies \alpha=1, \beta>1$
小结 $x \to 0^+$：积分收敛的条件是 $\alpha < 1$** 或者 **$\alpha=1, \beta>1$。这也与图片中的结论完全一致。

最终结论
一个形如 $\int_0^\infty \frac{1}{x^\alpha \ln^\beta x} dx$ 的积分要收敛，必须同时满足在 $0$ 附近和在 $\infty$ 附近的收敛条件。但由于这两个条件是矛盾的（一个要求 $\alpha<1$，一个要求 $\alpha>1$），所以这种形式的积分在 $[0, \infty]$ 上永远是发散的。但是这对于分析更复杂的、只含一个瑕点的积分问题至关重要。