## **周期函数与幂函数乘积的均值定理证明**

## **问题描述**

设 $f(t)$ 是以 $T$ 为周期的连续函数，且在 $[0, T]$ 上不变号（即 $f(t) \ge 0$ 或 $f(t) \le 0$ 对所有 $t \in [0, T]$ 成立）。求证：
$$
\lim_{x\rightarrow \infty} \frac{\int_0^x{t^n f(t) dt}}{\int_0^x{t^n dt}} = \frac{1}{T}\int_0^T f(t) dt
$$
其中 $n$ 为非负整数。

---

### **证明过程**

#### **第一步：将连续极限离散化**

我们的目标是分析 $x \rightarrow \infty$ 时的极限。可以将 $x$ 与其周期长度 $T$ 的整数倍关联起来。

令 $m = \lfloor \frac{x}{T} \rfloor$。根据下取整的定义，我们有：
$$ m \le \frac{x}{T} < m+1 $$
$$ mT \le x < (m+1)T $$
当 $x \rightarrow \infty$ 时，显然 $m \rightarrow \infty$。

我们可以证明 $x$ 与 $mT$ 是等价无穷大。由上式可得：
$$ 1 \le \frac{x}{mT} < \frac{m+1}{m} = 1 + \frac{1}{m} $$
根据夹逼定理，当 $m \rightarrow \infty$ 时：
$$ \lim_{m\rightarrow \infty} \frac{x}{mT} = 1 $$
因此，$\lim_{x\rightarrow \infty} \sim \lim_{m\rightarrow \infty} mT$。这意味着在求极限时，我们可以用 $mT$ 来替换 $x$。
> **注记**: 这一步的严谨性依赖于积分的性质。因为 $f(t)$ 不变号，分子和分母的积分都是单调的，用 $mT$ 替换 $x$ 所产生的误差是高阶无穷小，在极限中可以被忽略。

故原极限可转化为：
$$ \lim_{x\rightarrow \infty} \frac{\int_0^x{t^n f(t) dt}}{\int_0^x{t^n dt}} = \lim_{m\rightarrow \infty} \frac{\int_0^{mT}{t^n f(t) dt}}{\int_0^{mT}{t^n dt}} $$

#### **第二步：将积分拆分为区间和**

我们将整个积分区间 $[0, mT]$ 拆分为 $m$ 个长度为 $T$ 的子区间 $[kT, (k+1)T]$。

**分母计算**:
$$ \int_0^{mT} t^n dt = \frac{1}{n+1} (mT)^{n+1} $$

**分子处理**:
$$ \int_0^{mT} t^n f(t) dt = \sum_{k=0}^{m-1} \int_{kT}^{(k+1)T} t^n f(t) dt $$
在每个积分中，进行变量替换，令 $u = t - kT$，则 $t = u+kT$， $du = dt$。当 $t$ 从 $kT$ 变化到 $(k+1)T$ 时，$u$ 从 $0$ 变化到 $T$。利用 $f(t)$ 的周期性，$f(u+kT) = f(u)$。
$$ \int_0^{mT} t^n f(t) dt = \sum_{k=0}^{m-1} \int_0^T (u+kT)^n f(u) du $$

#### **第三步：应用积分中值定理**

对于求和中的每一项 $\int_0^T (u+kT)^n f(u) du$，我们可以应用**积分第一中值定理的推广形式（加权积分中值定理）**。

令 $g(u) = (u+kT)^n$ 和 $h(u) = f(u)$。
*   $g(u)$ 在 $[0, T]$ 上连续。
*   $h(u)=f(u)$ 在 $[0, T]$ 上可积且**不变号**（这是本证明的关键前提）。

因此，对于每一个 $k \in \{0, 1, \dots, m-1\}$，都存在一个 $\xi_k \in [0, T]$，使得：
$$ \int_0^T (u+kT)^n f(u) du = ( \xi_k + kT )^n \int_0^T f(u) du $$
> **注记**: 这里的 $\xi_k$ 依赖于 $k$，在不同的子区间上其值可能不同。

将此结果代回分子求和式：
$$ \sum_{k=0}^{m-1} \int_0^T (u+kT)^n f(u) du = \sum_{k=0}^{m-1} (\xi_k + kT)^n \int_0^T f(u) du = \left( \int_0^T f(t) dt \right) \sum_{k=0}^{m-1} (\xi_k + kT)^n $$

#### **第四步：构建黎曼和并求极限**

现在，我们将分子和分母代回极限表达式：
$$
\begin{aligned}
\text{原极限} &= \lim_{m\rightarrow \infty} \frac{\left( \int_0^T f(t) dt \right) \sum_{k=0}^{m-1} (\xi_k + kT)^n}{\frac{1}{n+1} (mT)^{n+1}} \\
&= (n+1) \frac{\int_0^T f(t) dt}{T^{n+1}} \lim_{m\rightarrow \infty} \frac{1}{m^{n+1}} \sum_{k=0}^{m-1} (kT+\xi_k)^n \\
&= (n+1) \frac{\int_0^T f(t) dt}{T} \lim_{m\rightarrow \infty} \frac{1}{m} \sum_{k=0}^{m-1} \left( \frac{kT+\xi_k}{mT} \right)^n \\
&= (n+1) \frac{\int_0^T f(t) dt}{T} \lim_{m\rightarrow \infty} \frac{1}{m} \sum_{k=0}^{m-1} \left( \frac{k + \xi_k/T}{m} \right)^n
\end{aligned}
$$
令 $F(m) = \frac{1}{m} \sum_{k=0}^{m-1} \left( \frac{k + \xi_k/T}{m} \right)^n$。
这个表达式是函数 $g(x) = x^n$ 在区间 $[0, 1]$ 上的黎曼和。
因为 $\xi_k \in [0, T]$，所以 $\alpha_k = \xi_k/T \in [0, 1]$。我们选取的点 $c_k = \frac{k+\alpha_k}{m}$ 位于子区间 $[\frac{k}{m}, \frac{k+1}{m}]$ 内。

因此，该极限就是定积分 $\int_0^1 x^n dx$：
$$ \lim_{m\rightarrow \infty} F(m) = \int_0^1 x^n dx = \left[ \frac{x^{n+1}}{n+1} \right]_0^1 = \frac{1}{n+1} $$

#### **第五步：最终结论**

将上一步的结果代回，我们得到：
$$
\text{原极限} = (n+1) \frac{\int_0^T f(t) dt}{T} \times \frac{1}{n+1} = \frac{1}{T} \int_0^T f(t) dt
$$
**证明完毕。**

------

### **使用 Stolz-Cesàro 定理的证明**

#### **Stolz-Cesàro 定理回顾**

Stolz-Cesàro 定理，常被称为“离散形式的洛必达法则”，其内容如下：

设有两个数列 $\{a_k\}$ 和 $\{b_k\}$。如果：
1.  $\{b_k\}$ 是一个严格单调递增且趋于 $+\infty$ 的数列。
2.  极限 $\lim_{k\rightarrow \infty} \frac{a_k - a_{k-1}}{b_k - b_{k-1}}$ 存在。

那么，极限 $\lim_{k\rightarrow \infty} \frac{a_k}{b_k}$ 也存在，并且二者相等：
$$
\lim_{k\rightarrow \infty} \frac{a_k}{b_k} = \lim_{k\rightarrow \infty} \frac{a_k - a_{k-1}}{b_k - b_{k-1}}
$$

---

### **证明过程**

#### **第一步：构造数列**

与之前的证明一样，我们首先将连续的极限问题离散化。我们不直接处理 $x \to \infty$，而是考察一个特定的序列 $x_m = mT$，$m \in \mathbb{N}$。如果当 $m \to \infty$ 时，我们能求出极限，那么对于一般的 $x \to \infty$，其极限值也相同（这一步的严谨证明可以补充，但此处我们先接受这个结论）。

我们定义两个数列：
$$ a_m = \int_0^{mT} t^n f(t) dt $$
$$ b_m = \int_0^{mT} t^n dt = \frac{(mT)^{n+1}}{n+1} $$

我们的目标是求 $\lim_{m\rightarrow\infty} \frac{a_m}{b_m}$。

#### **第二步：验证 Stolz-Cesàro 定理的条件**

1.  **验证 $\{b_m\}$ 的性质**:
    *   $b_m = \frac{T^{n+1}}{n+1} m^{n+1}$。由于 $T>0, n \ge 0$，当 $m$ 增加时，$b_m$ 严格单调递增。
    *   显然，$\lim_{m\rightarrow\infty} b_m = +\infty$。
    
    条件满足，我们可以应用 Stolz-Cesàro 定理。

#### **第三步：计算差分**

现在我们计算分子和分母的差分项 $a_m - a_{m-1}$ 和 $b_m - b_{m-1}$。

**分子差分**:
$$
\begin{aligned}
a_m - a_{m-1} &= \int_0^{mT} t^n f(t) dt - \int_0^{(m-1)T} t^n f(t) dt \\
&= \int_{(m-1)T}^{mT} t^n f(t) dt
\end{aligned}
$$

**分母差分**:
$$
\begin{aligned}
b_m - b_{m-1} &= \int_0^{mT} t^n dt - \int_0^{(m-1)T} t^n dt \\
&= \int_{(m-1)T}^{mT} t^n dt
\end{aligned}
$$

#### **第四步：计算差分比的极限**

根据 Stolz-Cesàro 定理，我们现在需要计算以下极限：
$$ \lim_{m\rightarrow\infty} \frac{a_m - a_{m-1}}{b_m - b_{m-1}} = \lim_{m\rightarrow\infty} \frac{\int_{(m-1)T}^{mT} t^n f(t) dt}{\int_{(m-1)T}^{mT} t^n dt} $$

为了处理这个极限，我们对分子和分母的积分进行变量替换。令 $t = u + (m-1)T$，则 $du=dt$。当 $t$ 从 $(m-1)T$ 变化到 $mT$ 时，$u$ 从 $0$ 变化到 $T$。

**分子变换**:
$$ \int_{(m-1)T}^{mT} t^n f(t) dt = \int_0^T (u+(m-1)T)^n f(u+(m-1)T) du $$
利用 $f(t)$ 的周期性，$f(u+(m-1)T) = f(u)$。
$$ = \int_0^T (u+(m-1)T)^n f(u) du $$

**分母变换**:
$$ \int_{(m-1)T}^{mT} t^n dt = \int_0^T (u+(m-1)T)^n du $$

现在，极限表达式变为：
$$ \lim_{m\rightarrow\infty} \frac{\int_0^T (u+(m-1)T)^n f(u) du}{\int_0^T (u+(m-1)T)^n du} $$
当 $m \to \infty$ 时，$u \in [0, T]$ 的值相对于 $(m-1)T$ 变得可以忽略。我们可以将主导项 $(m-1)T$ 提出来：
$$ (u+(m-1)T)^n = ((m-1)T)^n \left(1 + \frac{u}{(m-1)T}\right)^n $$
当 $m \to \infty$ 时，对于任意 $u \in [0, T]$，$\frac{u}{(m-1)T} \to 0$，因此 $\left(1 + \frac{u}{(m-1)T}\right)^n \to 1$。由于这个收敛在 $[0, T]$ 上是一致的，我们可以将极限与积分交换。

> **更严谨的论证**:
> 将表达式的主导部分提出：
> $$ \frac{((m-1)T)^n \int_0^T \left(1 + \frac{u}{(m-1)T}\right)^n f(u) du}{((m-1)T)^n \int_0^T \left(1 + \frac{u}{(m-1)T}\right)^n du} = \frac{\int_0^T \left(1 + \frac{u}{(m-1)T}\right)^n f(u) du}{\int_0^T \left(1 + \frac{u}{(m-1)T}\right)^n du} $$
> 因为 $f(u)$ 连续（因此在 $[0,T]$ 上有界），且 $\left(1 + \frac{u}{(m-1)T}\right)^n$ 在 $[0, T]$ 上一致收敛于 $1$，我们可以将极限移入积分号内：
> $$ \lim_{m\to\infty} \text{分子} = \int_0^T \lim_{m\to\infty} \left[\left(1 + \frac{u}{(m-1)T}\right)^n f(u)\right] du = \int_0^T f(u) du $$
> $$ \lim_{m\to\infty} \text{分母} = \int_0^T \lim_{m\to\infty} \left(1 + \frac{u}{(m-1)T}\right)^n du = \int_0^T 1 du = T $$

因此，差分比的极限为：
$$ \lim_{m\rightarrow\infty} \frac{a_m - a_{m-1}}{b_m - b_{m-1}} = \frac{\int_0^T f(u) du}{T} $$

#### **第五步：最终结论**

根据 Stolz-Cesàro 定理，原极限等于差分比的极限：
$$ \lim_{x\rightarrow \infty} \frac{\int_0^x{t^n f(t) dt}}{\int_0^x{t^n dt}} = \lim_{m\rightarrow \infty} \frac{a_m}{b_m} = \frac{1}{T} \int_0^T f(t) dt $$
**证明完毕。**