# 一维连续型随机变量函数分布公式的证明

### 1. 已知条件与目标

**已知**:
1.  `X` 是一个连续型随机变量，其概率密度函数 (PDF) 为 `f_X(x)`，累积分布函数 (CDF) 为 `F_X(x) = P(X ≤ x)`。
2.  随机变量 `Y` 是 `X` 的函数，`Y = g(X)`。
3.  函数 `y = g(x)` 在 `X` 的取值范围内是**严格单调**（严格递增或严格递减）且可导的。
4.  `g(x)` 的反函数存在，记为 `x = h(y)`。

**欲证**:
`Y` 的概率密度函数 `f_Y(y)` 为：
$$
f_Y(y) = f_X[h(y)] \cdot |h'(y)|
$$
其中 `h'(y)` 是 `h(y)` 对 `y` 的导数。

---

### 2. 证明思路

证明的核心思想是“**从定义出发**”。我们无法直接求出 PDF `f_Y(y)`，但我们可以先求出它的 CDF `F_Y(y)`，然后再通过求导得到 PDF。

1.  写出 `Y` 的 CDF 的定义： `F_Y(y) = P(Y ≤ y)`。
2.  将 `Y = g(X)` 代入，得到 `F_Y(y) = P(g(X) ≤ y)`。
3.  利用 `g(x)` 的单调性，将不等式 `g(X) ≤ y` 转化为关于 `X` 的不等式。
4.  将该概率表示为 `X` 的 CDF，即 `F_X(...)` 的形式。
5.  对 `F_Y(y)` 关于 `y` 求导，得到 `f_Y(y)`。

---

### 3. 证明过程

我们分两种情况讨论 `g(x)` 的单调性。

#### 情况一：`g(x)` 是严格单调递增函数

1.  根据 CDF 的定义：
    $$
    F_Y(y) = P(Y \le y) = P(g(X) \le y)
    $$

2.  因为 `g(x)` 是严格单调递增的，所以它的反函数 `h(y)` 也是严格单调递增的。对不等式 `g(X) ≤ y` 两边同时应用 `h` 函数，**不等号方向不变**：
    $$
    h(g(X)) \le h(y) \quad \implies \quad X \le h(y)
    $$

3.  因此，`Y` 的 CDF 可以用 `X` 的 CDF 来表示：
    $$
    F_Y(y) = P(X \le h(y)) = F_X(h(y))
    $$

4.  对上式两边关于 `y` 求导，以求得 `Y` 的 PDF。根据**链式求导法则**：
    $$
    f_Y(y) = \frac{d}{dy} F_Y(y) = \frac{d}{dy} F_X(h(y))
    $$
    $$
    f_Y(y) = F'_X(h(y)) \cdot h'(y)
    $$

5.  我们知道 `F'_X(x) = f_X(x)`，所以：
    $$
    f_Y(y) = f_X(h(y)) \cdot h'(y)
    $$

6.  因为 `g(x)`（以及其反函数 `h(y)`）是递增的，所以其导数 `h'(y) > 0`。因此，`h'(y) = |h'(y)|`。
    最终得到：
    $$
    \boldsymbol{f_Y(y) = f_X(h(y)) \cdot |h'(y)|}
    $$

---

#### 情况二：`g(x)` 是严格单调递减函数

1.  同样从 CDF 的定义开始：
    $$
    F_Y(y) = P(Y \le y) = P(g(X) \le y)
    $$

2.  因为 `g(x)` 是严格单调递减的，所以它的反函数 `h(y)` 也是严格单调递减的。对不等式 `g(X) ≤ y` 两边同时应用 `h` 函数，**不等号方向必须反转**：
    $$
    h(g(X)) \ge h(y) \quad \implies \quad X \ge h(y)
    $$

3.  因此，`Y` 的 CDF 变为：
    $$
    F_Y(y) = P(X \ge h(y))
    $$
    我们知道 `P(X ≥ a) = 1 - P(X < a)`。对于连续型随机变量，`P(X < a) = P(X ≤ a)`，所以：
    $$
    F_Y(y) = 1 - P(X \le h(y)) = 1 - F_X(h(y))
    $$

4.  对上式两边关于 `y` 求导：
    $$
    f_Y(y) = \frac{d}{dy} F_Y(y) = \frac{d}{dy} [1 - F_X(h(y))]
    $$
    $$
    f_Y(y) = 0 - F'_X(h(y)) \cdot h'(y) = -f_X(h(y)) \cdot h'(y)
    $$

5.  因为 `g(x)`（以及其反函数 `h(y)`）是递减的，所以其导数 `h'(y) < 0`。因此，`-h'(y)` 是一个正数，即 `-h'(y) = |h'(y)|`。
    最终得到：
    $$
    \boldsymbol{f_Y(y) = f_X(h(y)) \cdot |h'(y)|}
    $$

---

### 4. 结论

综合以上两种情况，无论 `g(x)` 是严格单调递增还是递减，`Y = g(X)` 的概率密度函数都可以用统一的公式表示：
$$
\Large f_Y(y) = f_X[h(y)] \cdot |h'(y)|
$$
证明完毕。

### 附注：`|h'(y)|` 的直观意义

`|h'(y)|` 在数学中被称为**雅可比行列式 (Jacobian)** 的绝对值（在一维情况下就是导数的绝对值）。它扮演了一个**概率密度缩放因子**的角色。

-   一段微小的 `dx` 区间内的概率约为 `f_X(x)dx`。
-   经过 `y = g(x)` 变换后，这段 `dx` 区间映射到了 `dy` 区间，其中 `dy ≈ g'(x)dx`，或者 `dx ≈ h'(y)dy`。
-   那么 `dy` 区间内的概率 `f_Y(y)dy` 应该等于原来 `dx` 区间的概率：
    $$
    f_Y(y)|dy| \approx f_X(x)|dx|
    $$
    $$
    f_Y(y)|dy| \approx f_X(h(y))|h'(y)dy|
    $$
    $$
    f_Y(y) \approx f_X(h(y))|h'(y)|
    $$
这直观地解释了为什么需要乘以导数的绝对值，它是为了保证在坐标系拉伸或压缩后，总概率守恒（积分仍为1）。