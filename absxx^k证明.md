### 证明

设函数 $g(x) = |x|x^k$，其中 $k$ 是一个非负整数。我们可以将其写为分段函数形式：
$$
g(x) =
\begin{cases}
x^{k+1}, & x \ge 0 \\
-x^{k+1}, & x < 0
\end{cases}
$$
并且显然 $g(0) = 0$。

我们的证明分为两部分：
1.  **第一部分：** 证明对于任意整数 $m$ 满足 $0 \le m \le k$，$g^{(m)}(0)$ 存在且等于 $0$。
2.  **第二部分：** 证明 $g^{(k+1)}(0)$ 不存在。

---

#### **第一部分：$g^{(m)}(0)=0$ 对于 $0 \le m \le k$**

我们使用数学归纳法。

**基础情况 ($m=0$):**
$g^{(0)}(0) = g(0) = 0$。结论成立。

**归纳假设:**
假设对于某个整数 $m-1$ 满足 $0 \le m-1 < k$，我们有 $g^{(m-1)}(0) = 0$。

**归纳步骤:**
我们需要计算 $g^{(m)}(0)$。根据导数定义：
$$ g^{(m)}(0) = \lim_{x \to 0} \frac{g^{(m-1)}(x) - g^{(m-1)}(0)}{x} $$
根据归纳假设，$g^{(m-1)}(0)=0$，所以：
$$ g^{(m)}(0) = \lim_{x \to 0} \frac{g^{(m-1)}(x)}{x} $$
为了计算这个极限，我们首先需要求出 $g^{(m-1)}(x)$ 对于 $x \neq 0$ 的表达式。
*   当 $x > 0$ 时，$g(x) = x^{k+1}$，其 $(m-1)$ 阶导数为：
    $$ g^{(m-1)}(x) = \frac{d^{m-1}}{dx^{m-1}} (x^{k+1}) = (k+1)k\cdots(k-m+3) x^{k-m+2} = P(k+1, m-1) x^{k-m+2} $$
*   当 $x < 0$ 时，$g(x) = -x^{k+1}$，其 $(m-1)$ 阶导数为：
    $$ g^{(m-1)}(x) = -\frac{d^{m-1}}{dx^{m-1}} (x^{k+1}) = -P(k+1, m-1) x^{k-m+2} $$
这里 $P(n,r)$ 是排列数。我们可以将上式统一写成：
$$ g^{(m-1)}(x) = \mathrm{sgn}(x) \cdot P(k+1, m-1) x^{k-m+2}, \quad \text{for } x \neq 0 $$
现在代入极限表达式：
$$ g^{(m)}(0) = \lim_{x \to 0} \frac{\mathrm{sgn}(x) \cdot P(k+1, m-1) x^{k-m+2}}{x} = P(k+1, m-1) \lim_{x \to 0} \left( \mathrm{sgn}(x) \cdot x^{k-m+1} \right) $$
因为我们是在 $m \le k$ 的条件下，所以指数 $k-m+1 \ge k-k+1 = 1$。
由于指数 $k-m+1 \ge 1$，$\lim_{x \to 0} x^{k-m+1} = 0$。而函数 $\mathrm{sgn}(x)$ 在 $x \to 0$ 的邻域内是有界的（$|\mathrm{sgn}(x)|=1$）。
因此，
$$ g^{(m)}(0) = P(k+1, m-1) \cdot 0 = 0 $$
归纳步骤完成。这就证明了对于所有 $0 \le m \le k$，$g^{(m)}(0)=0$。

---

#### **第二部分：$g^{(k+1)}(0)$ 不存在**

根据导数定义，我们来计算 $g^{(k+1)}(0)$：
$$ g^{(k+1)}(0) = \lim_{x \to 0} \frac{g^{(k)}(x) - g^{(k)}(0)}{x} $$
从第一部分的结论我们知道，$g^{(k)}(0)=0$。所以：
$$ g^{(k+1)}(0) = \lim_{x \to 0} \frac{g^{(k)}(x)}{x} $$
我们利用第一部分推导出的 $g^{(m-1)}(x)$ 的表达式，令 $m-1=k$，即 $m=k+1$，得到：
$$ g^{(k)}(x) = \mathrm{sgn}(x) \cdot P(k+1, k) x^{k-(k+1)+2} = \mathrm{sgn}(x) \cdot P(k+1, k) x^1 $$
我们知道 $P(k+1, k) = (k+1)!$。所以：
$$ g^{(k)}(x) = (k+1)! \cdot \mathrm{sgn}(x) \cdot x = (k+1)! \cdot |x| $$
将这个结果代入 $g^{(k+1)}(0)$ 的极限表达式：
$$ g^{(k+1)}(0) = \lim_{x \to 0} \frac{(k+1)! \cdot |x|}{x} = (k+1)! \lim_{x \to 0} \frac{|x|}{x} $$
现在我们考察左右极限：
*   右极限：
    $$ \lim_{x \to 0^+} \frac{|x|}{x} = \lim_{x \to 0^+} \frac{x}{x} = 1 $$
*   左极限：
    $$ \lim_{x \to 0^-} \frac{|x|}{x} = \lim_{x \to 0^-} \frac{-x}{x} = -1 $$
由于左右极限不相等，即 $\lim_{x \to 0^+} \frac{|x|}{x} \neq \lim_{x \to 0^-} \frac{|x|}{x}$，所以极限 $\lim_{x \to 0} \frac{|x|}{x}$ 不存在。

因此，$g^{(k+1)}(0)$ 不存在。

**综上所述，函数 $g(x)=|x|x^k$ 在 $x=0$ 处是 $k$ 阶可导的，但其 $(k+1)$ 阶导数在 $x=0$ 处不存在。**

**证毕。**