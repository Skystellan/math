# 微分算子与欧拉算子

## 一、微分算子 (D)

### 1. 定义与基本性质

微分算子 $D$ 表示对变量（通常是 $x$）的求导运算：
$$ D = \frac{d}{dx} $$
高阶微分算子表示为：
$$ D^n = \frac{d^n}{dx^n} $$
以 $D$ 为不定元，常数为系数的多项式 $P(D) = a_n D^n + a_{n-1}D^{n-1} + \dots + a_1 D + a_0$ 称为 $n$ 阶线性常系数微分算子。作用在函数 $y=y(x)$ 上表示为：
$$ P(D)y = (a_n D^n + a_{n-1}D^{n-1} + \dots + a_1 D + a_0)y = a_n y^{(n)} + a_{n-1}y^{(n-1)} + \dots + a_1 y' + a_0 y $$
**基本性质**：
*   **线性性**：$P(D)(c_1 y_1 + c_2 y_2) = c_1 P(D)y_1 + c_2 P(D)y_2$，其中 $c_1, c_2$ 为常数。

### 2. 微分算子的移位定理 (Shift Theorem)

移位定理是利用微分算子求解微分方程的强大工具。
我们可以定义一个乘法算子 $M_a$ 为 $M_a\{y\} = e^{ax}y$。

**定理1 (指数移位定理)**：设 $P(D)$ 是 $D$ 的多项式，则
$$ P(D) \{e^{ax} y\} = e^{ax} P(D+a) \{y\} $$
用乘法算子 $M_a$ 表示，即：
$$ P(D) M_a = M_a P(D+a) $$
**推导**：
首先验证对于算子 $D$ 和 $M_a$：
$D M_a \{y\} = D\{e^{ax}y\} = \frac{d}{dx}(e^{ax}y) = ae^{ax}y + e^{ax}y' = e^{ax}(ay + y') = M_a\{(aI+D)y\} = M_a(D+aI)\{y\}$
（通常 $aI$ 中的 $I$ 作为恒等算子可以省略，写成 $M_a(D+a)\{y\}$）
所以，算子关系为 $D M_a = M_a (D+a)$。

然后验证 $D^2 M_a$：
$D^2 M_a = D (D M_a) = D (M_a (D+a))$
利用已证的 $D M_a = M_a (D+a)$，我们可以将其看作 $D$ 作用在 $M_a ((D+a)y)$ 上。
或者更清晰地，将 $(D+a)$ 视为一个新的算子 $Q_1 = D+a$。
$D (M_a Q_1) = (D M_a) Q_1 = (M_a (D+a)) Q_1 = M_a (D+a) (D+a) = M_a (D+a)^2$。
所以 $D^2 M_a = M_a (D+a)^2$。

通过数学归纳法，可以证明对于任意正整数 $n$：
$$ D^n M_a = M_a (D+a)^n $$
由于 $P(D) = \sum_{k=0}^{n} c_k D^k$ 是 $D^k$ 的线性组合，根据算子的线性性：
$P(D) M_a = (\sum c_k D^k) M_a = \sum c_k (D^k M_a) = \sum c_k (M_a (D+a)^k)$
$= M_a (\sum c_k (D+a)^k) = M_a P(D+a)$。
定理成立。

**定理2 (指数移位定理的另一种形式)**：
$$ e^{ax} P(D) \{y\} = P(D-a) \{e^{ax} y\} $$
用乘法算子 $M_a$ 表示，即：
$$ M_a P(D) = P(D-a) M_a $$
**推导**：
根据定理1，我们有 $P(D) M_a = M_a P(D+a)$。
用 $P(D-a)$ 替换 $P(D)$，则 $P(D-a) M_a = M_a P((D-a)+a) = M_a P(D)$。
即 $P(D-a) M_a = M_a P(D)$。
两边交换顺序即得 $M_a P(D) = P(D-a) M_a$。
定理成立。

### 3. 微分算子在常系数线性微分方程中的应用 (基于算子分解与移位)

考虑 $n$ 阶常系数线性非齐次微分方程：
$$ P(D)y = f(x) $$
其中 $P(D) = (D-\lambda_1)(D-\lambda_2)\dots(D-\lambda_n)$ (这里假设特征根为 $\lambda_1, \dots, \lambda_n$，可以是重复的)。
特解 $y^*$ 可以形式地写为 $y^* = [P(D)]^{-1}f(x) = [(D-\lambda_n)]^{-1} \dots [(D-\lambda_2)]^{-1} [(D-\lambda_1)]^{-1} f(x)$。
核心算子关系为：
$$ (D-\lambda)^{-1} \{g(x)\} = M_\lambda D^{-1} M_{-\lambda} \{g(x)\} = e^{\lambda x} \int e^{-\lambda x} g(x) dx $$
其中 $D^{-1}$ 表示一次积分 $\int (\cdot) dx$ (通常不加积分常数，因为我们求的是特解)。

#### (1) $f(x) = e^{kx}$ 型 (且 $k$ 不是特征根)
如果 $P(D)y = e^{kx}$ 且 $P(k) \neq 0$。
$y^* = [P(D)]^{-1} \{e^{kx}\}$
利用 $P(D)e^{kx} = P(k)e^{kx}$，如果 $P(k) \neq 0$，则 $P(D)^{-1}e^{kx} = \frac{1}{P(k)}e^{kx}$。
这个结果可以直接得到，无需复杂的算子分解。
**例**：求解 $y'' - 3y' + 2y = e^{3x}$ 的特解。
$P(D) = D^2 - 3D + 2 = (D-1)(D-2)$。$k=3$。
$P(3) = 3^2 - 3(3) + 2 = 9 - 9 + 2 = 2 \neq 0$。
$y^* = \frac{1}{P(3)}e^{3x} = \frac{1}{2}e^{3x}$。


#### (2) $f(x) = e^{kx}$ 型 (且 $k$ 是 $m$ 重特征根)
设 $P(D) = (D-k)^m Q(D)$，其中 $Q(k) \neq 0$。
$y^* = [(D-k)^m Q(D)]^{-1} \{e^{kx}\} = [Q(D)]^{-1} (D-k)^{-m} \{e^{kx}\}$
首先计算 $A(x) = (D-k)^{-m} \{e^{kx}\}$。
$(D-k)^{-1} \{e^{kx}\} = M_k D^{-1} M_{-k} \{e^{kx}\} = e^{kx} \int e^{-kx} e^{kx} dx = e^{kx} \int 1 dx = x e^{kx}$。
$(D-k)^{-2} \{e^{kx}\} = (D-k)^{-1} \{x e^{kx}\} = M_k D^{-1} M_{-k} \{x e^{kx}\} = e^{kx} \int e^{-kx} x e^{kx} dx = e^{kx} \int x dx = \frac{x^2}{2} e^{kx}$。
以此类推，$(D-k)^{-m} \{e^{kx}\} = \frac{x^m}{m!} e^{kx}$。
所以 $y^* = [Q(D)]^{-1} \left\{\frac{x^m}{m!} e^{kx}\right\} = \frac{1}{Q(k)} \frac{x^m}{m!} e^{kx}$。
(因为 $Q(D)$ 作用于 $e^{kx}$ 相当于乘以 $Q(k)$，所以 $[Q(D)]^{-1}$ 作用于 $e^{kx}$ 相当于乘以 $1/Q(k)$)。
**例**：求解 $y'' - 2y' + y = e^x$ 的特解。
$P(D) = D^2 - 2D + 1 = (D-1)^2$。$k=1$ 是2重根 ($m=2$)。
这里 $Q(D) = 1$ (或者说 $Q(k)=1$ )。
$y^* = \frac{1}{1} \frac{x^2}{2!} e^x = \frac{x^2}{2}e^x$。


#### (3) $f(x) = P_m(x)$ ( $m$ 次多项式) 型
$y^* = [P(D)]^{-1} \{P_m(x)\}$。
如果 $P(D)$ 的常数项不为零 (即 $D=0$ 不是特征根)，可以将 $[P(D)]^{-1}$ 按 $D$ 的升幂展开（泰勒展开）：
$[P(D)]^{-1} = c_0 + c_1 D + c_2 D^2 + \dots + c_m D^m + \dots$
然后作用于 $P_m(x)$，由于 $D^{k}P_m(x) = 0$ 当 $k > m$ 时，所以只需要展开到 $D^m$ 项。
例如，若 $P(D) = D-\lambda$ ($\lambda \neq 0$)，
$y^* = (D-\lambda)^{-1} \{P_m(x)\} = M_\lambda D^{-1} M_{-\lambda} \{P_m(x)\} = e^{\lambda x} \int e^{-\lambda x} P_m(x) dx$。
这个积分可以通过分部积分计算 $m$ 次。
或者，$(D-\lambda)^{-1} = -\frac{1}{\lambda}(1-\frac{D}{\lambda})^{-1} = -\frac{1}{\lambda}(1 + \frac{D}{\lambda} + \frac{D^2}{\lambda^2} + \dots)$。
**例**：求解 $y' - 2y = x^2$ 的特解。
$P(D) = D-2$。
$y^* = (D-2)^{-1} \{x^2\} = -\frac{1}{2}(1-\frac{D}{2})^{-1} \{x^2\}$
$= -\frac{1}{2}(1 + \frac{D}{2} + \frac{D^2}{4} + \dots) \{x^2\}$
$= -\frac{1}{2}(x^2 + \frac{1}{2}D(x^2) + \frac{1}{4}D^2(x^2))$
$= -\frac{1}{2}(x^2 + \frac{1}{2}(2x) + \frac{1}{4}(2))$
$= -\frac{1}{2}(x^2 + x + \frac{1}{2}) = -\frac{1}{2}x^2 - \frac{1}{2}x - \frac{1}{4}$。
或者用积分法：
$y^* = (D-2)^{-1}\{x^2\} = M_2 D^{-1} M_{-2} \{x^2\} = e^{2x} \int e^{-2x} x^2 dx$
$\int x^2 e^{-2x} dx = x^2 (-\frac{1}{2}e^{-2x}) - \int 2x (-\frac{1}{2}e^{-2x}) dx$
$= -\frac{1}{2}x^2 e^{-2x} + \int x e^{-2x} dx$
$= -\frac{1}{2}x^2 e^{-2x} + [x(-\frac{1}{2}e^{-2x}) - \int 1 \cdot (-\frac{1}{2}e^{-2x}) dx]$
$= -\frac{1}{2}x^2 e^{-2x} - \frac{1}{2}x e^{-2x} + \frac{1}{2} \int e^{-2x} dx$
$= -\frac{1}{2}x^2 e^{-2x} - \frac{1}{2}x e^{-2x} - \frac{1}{4}e^{-2x}$
$y^* = e^{2x} (-\frac{1}{2}x^2 e^{-2x} - \frac{1}{2}x e^{-2x} - \frac{1}{4}e^{-2x}) = -\frac{1}{2}x^2 - \frac{1}{2}x - \frac{1}{4}$。

#### (4) $f(x) = e^{kx} P_m(x)$ 型
$y^* = [P(D)]^{-1} \{e^{kx} P_m(x)\}$
利用移位定理 $P(D)M_k = M_k P(D+k)$，则 $[P(D)]^{-1}M_k = M_k [P(D+k)]^{-1}$。
所以 $y^* = M_k [P(D+k)]^{-1} \{P_m(x)\} = e^{kx} \left( [P(D+k)]^{-1} \{P_m(x)\} \right)$。
问题转化为求解 $Y_0^* = [P(D+k)]^{-1} \{P_m(x)\}$，这变为了类型 (3) 的问题，其中新的微分算子是 $P_*(D) = P(D+k)$。
例如，若 $P(D) = D-\lambda_1$，则 $P(D+k) = D+k-\lambda_1$。
$y^* = e^{kx} (D+k-\lambda_1)^{-1} \{P_m(x)\}$
$= e^{kx} M_{\lambda_1-k} D^{-1} M_{-(\lambda_1-k)} \{P_m(x)\}$
$= e^{kx} e^{(\lambda_1-k)x} \int e^{-(\lambda_1-k)x} P_m(x) dx = e^{\lambda_1 x} \int e^{(k-\lambda_1)x} P_m(x) dx$。
**例**：求解 $y' - y = x e^{2x}$ 的特解。
$P(D) = D-1$。$k=2$，$P_m(x) = x$。
$y^* = e^{2x} [ (D+2)-1 ]^{-1} \{x\} = e^{2x} (D+1)^{-1} \{x\}$
$(D+1)^{-1} \{x\} = (1+D)^{-1} \{x\} = (1 - D + D^2 - \dots) \{x\} = (1-D)\{x\} = x - D(x) = x-1$。
所以 $y^* = e^{2x}(x-1)$。

#### (5) $f(x) = e^{kx} \sin(\omega x)$ 或 $f(x) = e^{kx} \cos(\omega x)$ 型
利用欧拉公式，例如 $e^{kx}\sin(\omega x) = \text{Im}(e^{(k+i\omega)x})$。
先求解 $Y_c^* = [P(D)]^{-1} \{e^{(k+i\omega)x}\}$。
这变为了类型 (1) 或 (2) 的问题，其中指数为复数 $\alpha = k+i\omega$。
如果 $P(k+i\omega) \neq 0$，则 $Y_c^* = \frac{1}{P(k+i\omega)} e^{(k+i\omega)x}$。
则原方程的特解 $y^* = \text{Im}(Y_c^*)$。
如果 $k+i\omega$ 是 $m$ 重特征根，则 $Y_c^* = \frac{x^m}{P^{(m)}(k+i\omega)} e^{(k+i\omega)x}$ (或者使用算子分解法)。
然后取虚部或实部。
**例**：求解 $y'' + y = \cos(2x)$ 的特解。
$P(D) = D^2+1$。$k=0, \omega=2$。
考虑 $Y_c^* = (D^2+1)^{-1} \{e^{i2x}\}$。
$P(i2) = (i2)^2 + 1 = -4 + 1 = -3 \neq 0$。
$Y_c^* = \frac{1}{-3} e^{i2x} = -\frac{1}{3}(\cos(2x) + i\sin(2x))$。
$y^* = \text{Re}(Y_c^*) = -\frac{1}{3}\cos(2x)$。

**例2 (含指数)**：求解 $y' - y = e^x \cos x$ 的特解。
$P(D) = D-1$。$f(x) = e^x \cos x = \text{Re}(e^{(1+i)x})$。
考虑 $Y_c^* = (D-1)^{-1} \{e^{(1+i)x}\}$。
令 $\alpha = 1+i$。$P(\alpha) = (1+i)-1 = i \neq 0$。
$Y_c^* = \frac{1}{i} e^{(1+i)x} = -i e^x (\cos x + i\sin x) = -i e^x \cos x + e^x \sin x$。
$y^* = \text{Re}(Y_c^*) = e^x \sin x$。


**一般分解方法举例**：
求解 $(D-\lambda_2)(D-\lambda_1)y = f(x)$
$y^* = (D-\lambda_2)^{-1} \{ (D-\lambda_1)^{-1} \{f(x)\} \}$
令 $g(x) = (D-\lambda_1)^{-1} \{f(x)\} = e^{\lambda_1 x} \int e^{-\lambda_1 x} f(x) dx$。
则 $y^* = (D-\lambda_2)^{-1} \{g(x)\} = e^{\lambda_2 x} \int e^{-\lambda_2 x} g(x) dx$
$$ y^* = e^{\lambda_2 x} \int e^{-\lambda_2 x} \left( e^{\lambda_1 x} \int e^{-\lambda_1 x} f(x) dx \right) dx $$
$$ y^* = e^{\lambda_2 x} \int e^{(\lambda_1-\lambda_2)x} \left( \int e^{-\lambda_1 x} f(x) dx \right) dx $$
这种方法对于任何 $f(x)$ 都是适用的，只是积分可能复杂。


## 二、欧拉算子 ($\theta$ 或 $xD$)

### 1. 定义与基本性质

欧拉算子 $\theta$ 定义为：
$$ \theta = x D = x \frac{d}{dx} $$
**基本性质**：
*   作用于幂函数：$\theta x^k = x \frac{d}{dx}(x^k) = x (kx^{k-1}) = kx^k$。
    这表明 $x^k$ 是算子 $\theta$ 的特征函数，特征值为 $k$。
*   $\theta (uv) = x(u'v + uv') = (xu')v + u(xv') = (\theta u)v + u(\theta v)$ (这不完全是标准的乘法法则，需要注意)

### 2. 欧拉算子与高阶微分的关系

我们可以将 $x^n D^n$ 用 $\theta$ 的多项式表示：
*   $xD = \theta$
*   $x^2 D^2 = x^2 \frac{d^2}{dx^2}$
    我们有 $\theta y = xy'$。
    $\theta^2 y = \theta(\theta y) = xD(xy') = x(y' + xy'') = xy' + x^2 y'' = \theta y + x^2 D^2 y$
    所以 $x^2 D^2 = \theta^2 - \theta = \theta(\theta-1)$。
*   $x^3 D^3 = \theta(\theta-1)(\theta-2)$
*   一般地，可以证明：
    $$ x^n D^n = \theta(\theta-1)(\theta-2)\dots(\theta-n+1) $$

**推导** (一种思路)：
使用数学归纳法。
当 $n=1$ 时，$xD = \theta$，成立。
假设 $x^k D^k = \theta(\theta-1)\dots(\theta-k+1)$ 成立。
考虑 $x^{k+1}D^{k+1}y = x \cdot (x^k D^k) (Dy)$。
或者更直接地：
$x^n D^n y = \theta_n y$ (记 $\theta_n = \theta(\theta-1)\dots(\theta-n+1)$)
$\theta_{n+1} y = \theta (\theta_n y) - n \theta_n y$ (这个关系需要证明，或者使用 $x=e^t$ 变换)

**通过变量代换 $x = e^t$ (即 $t = \ln x$) 推导**：
令 $x = e^t$，则 $\frac{dt}{dx} = \frac{1}{x}$。
$Dy = \frac{dy}{dx} = \frac{dy}{dt} \frac{dt}{dx} = \frac{1}{x} \frac{dy}{dt}$
所以 $xD y = \frac{dy}{dt}$。如果记 $\mathcal{D} = \frac{d}{dt}$，则 $\theta = xD = \mathcal{D}$。

现在看二阶：
$D^2 y = \frac{d}{dx}\left(\frac{1}{x}\frac{dy}{dt}\right) = -\frac{1}{x^2}\frac{dy}{dt} + \frac{1}{x} \frac{d}{dx}\left(\frac{dy}{dt}\right)$
$= -\frac{1}{x^2}\frac{dy}{dt} + \frac{1}{x} \frac{d^2y}{dt^2}\frac{dt}{dx} = -\frac{1}{x^2}\frac{dy}{dt} + \frac{1}{x^2}\frac{d^2y}{dt^2}$
所以 $x^2 D^2 y = \frac{d^2y}{dt^2} - \frac{dy}{dt} = (\mathcal{D}^2 - \mathcal{D})y = \mathcal{D}(\mathcal{D}-1)y$。
由于 $\theta = \mathcal{D}$，所以 $x^2 D^2 = \theta(\theta-1)$。

一般地，通过这种代换可以证明 $x^n D^n = \mathcal{D}(\mathcal{D}-1)\dots(\mathcal{D}-n+1) = \theta(\theta-1)\dots(\theta-n+1)$。

### 3. 欧拉算子在欧拉方程中的应用

欧拉方程 (Cauchy-Euler Equation) 是一类形如：
$$ a_n x^n y^{(n)} + a_{n-1}x^{n-1}y^{(n-1)} + \dots + a_1 xy' + a_0 y = f(x) $$
其中 $a_i$ 是常数。

使用欧拉算子，可以将各项 $a_k x^k D^k y$ 替换为 $a_k \theta(\theta-1)\dots(\theta-k+1)y$。
例如，二阶欧拉方程：
$$ ax^2 y'' + bxy' + cy = f(x) $$
可以写成：
$$ [a\theta(\theta-1) + b\theta + c]y = f(x) $$
令 $Q(\theta) = a\theta(\theta-1) + b\theta + c$。这是一个关于 $\theta$ 的代数多项式。
方程变为 $Q(\theta)y = f(x)$。

**解法1：直接使用欧拉算子**
如果 $f(x) = x^k$ 型，我们可以尝试特解 $y^* = A x^k$。
代入 $Q(\theta)y = x^k$，得到 $Q(k) A x^k = x^k$。
若 $Q(k) \neq 0$，则 $A = \frac{1}{Q(k)}$，所以 $y^* = \frac{1}{Q(k)}x^k$。
若 $Q(k) = 0$，即 $k$ 是特征方程 $Q(r)=0$ 的 $m$ 重根，则特解形式为 $y^* = A (\ln x)^m x^k$ (A 通过代入原方程确定，或者使用下面的移位定理)。

**例1 ($f(x)=x^k$, $Q(k) \neq 0$)**: 求解 $x^2 y'' - 2xy' + 2y = x^3$ 的特解。
$a=1, b=-2, c=2$。
$Q(\theta) = \theta(\theta-1) - 2\theta + 2 = \theta^2 - \theta - 2\theta + 2 = \theta^2 - 3\theta + 2$。
$f(x) = x^3$，所以 $k=3$。
$Q(3) = 3^2 - 3(3) + 2 = 9 - 9 + 2 = 2 \neq 0$。
特解 $y^* = \frac{1}{Q(3)}x^3 = \frac{1}{2}x^3$。

**例2 ($f(x)=x^k$, $Q(k) = 0$)**: 求解 $x^2 y'' - xy' + y = x$ 的特解。
$Q(\theta) = \theta(\theta-1) - \theta + 1 = \theta^2 - 2\theta + 1 = (\theta-1)^2$。
$f(x) = x = x^1$，所以 $k=1$。
$Q(1) = (1-1)^2 = 0$。$k=1$ 是特征方程 $Q(r)=(r-1)^2=0$ 的2重根 ($m=2$)。
设特解 $y^* = A (\ln x)^2 x^1$。
或者使用下面的移位定理方法。

**解法2：变量代换 $x = e^t$**
如前所述，代换 $x=e^t$ (或 $t=\ln x$)，并且 $x^k D^k = \mathcal{D}(\mathcal{D}-1)\dots(\mathcal{D}-k+1)$，其中 $\mathcal{D} = \frac{d}{dt}$。
欧拉方程就转换为一个以 $t$ 为自变量的常系数线性微分方程：
$$ [a_n \mathcal{D}(\mathcal{D}-1)\dots(\mathcal{D}-n+1) + \dots + a_1 \mathcal{D} + a_0] Y(t) = F(t) $$
其中 $Y(t) = y(e^t)$，$F(t) = f(e^t)$。
这个常系数方程可以用前面讨论的微分算子 $D$ (这里是 $\mathcal{D}$) 的方法求解。得到 $Y(t)$ 后，再用 $t = \ln x$ 代回得到 $y(x)$。

**例3 (变量代换法)**: 求解 $x^2 y'' - xy' + y = \ln x$。
$Q(\theta) = (\theta-1)^2$。
令 $x=e^t$, $t=\ln x$。$\theta = \mathcal{D} = \frac{d}{dt}$。
方程变为 $(\mathcal{D}-1)^2 Y(t) = t$，其中 $Y(t) = y(e^t)$，$f(e^t) = \ln(e^t) = t$。
这是一个常系数线性微分方程。
齐次方程 $(\mathcal{D}-1)^2 Y(t) = 0$ 的特征根为 $r=1$ (二重)，齐次解 $Y_h(t) = (c_1 + c_2 t)e^t$。
求特解 $Y_p^*(t) = \frac{1}{(\mathcal{D}-1)^2} \{t\}$。
$\frac{1}{(\mathcal{D}-1)^2} = (1-\mathcal{D})^{-2} = (1 + 2\mathcal{D} + 3\mathcal{D}^2 + \dots)$。
$Y_p^*(t) = (1+2\mathcal{D})\{t\} = t + 2\mathcal{D}(t) = t+2$。
所以 $Y(t) = (c_1 + c_2 t)e^t + t+2$。
代回 $t=\ln x$，$e^t=x$：
$y(x) = (c_1 + c_2 \ln x)x + \ln x + 2$。
特解是 $y^* = \ln x + 2$。

**齐次欧拉方程的解**：
对于齐次方程 $[a\theta(\theta-1) + b\theta + c]y = 0$，其特征方程为 $ar(r-1) + br + c = 0$。
1.  若有两个不相等的实根 $r_1, r_2$，则通解为 $y_h = c_1 x^{r_1} + c_2 x^{r_2}$。
    **例**: $x^2y'' - 2y = 0 \implies \theta(\theta-1) - 2 = 0 \implies \theta^2-\theta-2=0 \implies (\theta-2)(\theta+1)=0$。
    $r_1=2, r_2=-1$。$y_h = c_1 x^2 + c_2 x^{-1}$。
2.  若有两个相等的实根 $r_1 = r_2 = r$，则通解为 $y_h = (c_1 + c_2 \ln x)x^r$。
    **例**: $x^2y'' - xy' + y = 0 \implies (\theta-1)^2 = 0$。
    $r=1$ (二重)。$y_h = (c_1 + c_2 \ln x)x$。
3.  若有一对共轭复根 $r_{1,2} = \alpha \pm i\beta$，则通解为 $y_h = x^\alpha [c_1 \cos(\beta \ln x) + c_2 \sin(\beta \ln x)]$。
    **例**: $x^2y'' + xy' + y = 0 \implies \theta(\theta-1) + \theta + 1 = 0 \implies \theta^2+1=0$。
    $r = \pm i$ ($\alpha=0, \beta=1$)。$y_h = x^0 [c_1 \cos(\ln x) + c_2 \sin(\ln x)] = c_1 \cos(\ln x) + c_2 \sin(\ln x)$。

### 4. 欧拉算子的移位定理 (类似)

对于欧拉算子 $\theta = xD$，也有类似的移位性质，尤其是在与 $x^k$ 作用时。
$Q(\theta) \{x^k Y\} = x^k Q(\theta+k) \{Y\}$
**推导**：
验证 $\theta \{x^k Y\}$:
$\theta \{x^k Y\} = xD(x^k Y) = x(kx^{k-1}Y + x^k Y') = kx^k Y + x^{k+1}Y'$
$= kx^k Y + x^k (xY') = x^k (kY + \theta Y) = x^k (\theta+k)Y$
通过归纳可以推广到 $Q(\theta)$。

这个性质在求解 $Q(\theta)y = x^k P_m(\ln x)$ 型的特解时非常有用。
令 $y^* = x^k Z(\ln x)$，代入方程：
$Q(\theta)\{x^k Z\} = x^k Q(\theta+k)\{Z\} = x^k P_m(\ln x)$
所以 $Q(\theta+k)\{Z\} = P_m(\ln x)$。
令 $t = \ln x$，$\mathcal{D}_t = \frac{d}{dt}$。则 $Q(\mathcal{D}_t+k)\{Z(t)\} = P_m(t)$。
这又化为了一个关于 $Z(t)$ 的常系数线性微分方程。

**例4 (欧拉算子移位定理)**: 求解 $x^2 y'' - xy' + y = x (\ln x)^2$ 的特解。
$Q(\theta) = (\theta-1)^2$。$f(x) = x (\ln x)^2$，所以 $k=1$，$P_m(\ln x) = (\ln x)^2$。
令 $y^* = x^1 Z(\ln x)$。
$Q(\theta+1)\{Z\} = (\ln x)^2$
$Q(\theta+1) = ((\theta+1)-1)^2 = \theta^2$。
所以 $\theta^2 \{Z(\ln x)\} = (\ln x)^2$。
令 $t = \ln x$，$\mathcal{D}_t = \frac{d}{dt}$。方程变为 $\mathcal{D}_t^2 \{Z(t)\} = t^2$。
$\mathcal{D}_t Z(t) = \int t^2 dt = \frac{t^3}{3} + C_1$。
$Z(t) = \int (\frac{t^3}{3} + C_1) dt = \frac{t^4}{12} + C_1 t + C_2$。
取特解 $Z(t) = \frac{t^4}{12}$。
所以 $y^* = x \frac{(\ln x)^4}{12}$。

回到 **例2 ($f(x)=x^k$, $Q(k) = 0$)**: 求解 $x^2 y'' - xy' + y = x$ 的特解。
$Q(\theta) = (\theta-1)^2$。$f(x) = x = x^1 \cdot (\ln x)^0$。$k=1$，$P_m(\ln x) = 1$ (0次多项式)。
令 $y^* = x^1 Z(\ln x)$。
$Q(\theta+1)\{Z\} = 1 \implies \theta^2 \{Z(\ln x)\} = 1$。
令 $t = \ln x$，$\mathcal{D}_t^2 \{Z(t)\} = 1$。
$\mathcal{D}_t Z(t) = t + C_1$。
$Z(t) = \frac{t^2}{2} + C_1 t + C_2$。
取特解 $Z(t) = \frac{t^2}{2}$。
所以 $y^* = x \frac{(\ln x)^2}{2}$。
这与之前设 $y^* = A (\ln x)^m x^k$ 中 $m=2$ 的形式一致，并且确定了系数。
