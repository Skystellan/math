# 函数组合的连续性与间断性判定法则

## 一、四则运算的连续性

设函数 `f(x)` 和 `φ(x)` 在点 `x=a` 的性质已知。

### 1. 连续函数与连续函数
若 `f(x)` 和 `φ(x)` 在 `x=a` 均连续：
- **和/差**: `f(x) ± φ(x)` → **一定连续**。
- **积**: `f(x) · φ(x)` → **一定连续**。
- **商**: `f(x) / φ(x)` → **一定连续** (前提: `φ(a) ≠ 0`)。

### 2. 连续函数与间断函数
若 `f(x)` 在 `x=a` 连续，`φ(x)` 在 `x=a` 间断：
- **和/差**: `f(x) ± φ(x)` → **一定间断**。
  > **证明 (反证法):**
  > 假设 `h(x) = f(x) + φ(x)` 连续。
  > 则 `φ(x) = h(x) - f(x)`。
  > 右侧为两连续函数之差，结果必连续，与 `φ(x)` 间断矛盾。

- **积**: `f(x) · φ(x)` → **不确定**。
  - **可能连续**: 当且仅当 `f(a)=0` 时。零点可以"吸收"间断。
    > **例**: `f(x)=x`, `φ(x)=sin(1/x)`。在 `x=0` 点, `f(0)=0`，乘积 `x·sin(1/x)` 连续。
  - **一定间断**: 当 `f(a) ≠ 0` 时。

- **商**:
  - `φ(x) / f(x)` (间断/连续) → **一定间断** (前提: `f(a) ≠ 0`)。
  - `f(x) / φ(x)` (连续/间断) → **不确定**。
    > **例**: `f(x)=x`, `φ(x)=1/x`。在 `x=0` 点，商 `f(x)/φ(x) = x²` 连续。

### 3. 间断函数与间断函数
若 `f(x)` 和 `φ(x)` 在 `x=a` 均间断，则它们的和、差、积、商的连续性均 **不确定**。
> **例 (间断性相互抵消):**
> 设 `φ(x) = { 1, x ≥ 0; -1, x < 0 }`，`f(x) = -φ(x)`。两者都在 `x=0` 间断。
> - **和**: `f(x) + φ(x) = 0` (连续)。
>
> 设 `g(x) = { 2, x ≥ 0; 1/2, x < 0 }`，`h(x) = { 1/2, x ≥ 0; 2, x < 0 }`。两者都在 `x=0` 间断。
> - **积**: `g(x) · h(x) = 1` (连续)。

---

## 二、复合函数 `F(x) = f(g(x))` 的连续性

设内函数为 `g(x)`，外函数为 `f(u)`。

### 1. 内连续，外连续
- **结论**: 复合函数 `f(g(x))` **一定连续**。

### 2. 内连续，外间断
- **结论**: **不确定**。
- **判定关键**: 内函数 `g(x)` 的值域是否 **避开** 了外函数 `f(u)` 的间断点。
  - **可能连续**: 若 `g(x)` 在 `x=a` 附近的取值范围不包含 `f(u)` 的任何间断点。
    > **例**: `g(x)=x²+1`, `f(u)=1/u` (在u=0间断)。复合函数 `f(g(x))=1/(x²+1)` 处处连续。

### 3. 内间断，外连续
- **结论**: **不确定**。
- **判定关键**: 外函数 `f(u)` 是否能 **粘合** 内函数 `g(x)` 的间断。
  - **可能连续**: 若 `g(x)` 在 `x=a` 的左极限、右极限、函数值（可能不同）经过 `f(u)` 映射后，变为同一个输出值。
    > **例**: `g(x) = { 1, x ≥ 0; -1, x < 0 }`, `f(u)=u²`。在 `x=0` 点，`g(x)` 从-1跳到1，但 `f(-1)=f(1)=1`。复合函数 `f(g(x))` 恒为1，是连续的。

### 4. 内间断，外间断
- **结论**: **不确定**。情况最复杂，需要具体问题具体分析。

---

## 三、总结与解题策略

### 核心法则表

| 组合类型 | 条件 | 结论 | 关键点 |
| :--- | :--- | :--- | :--- |
| **四则运算** | 连续 $\circ$ 连续 | **一定连续** | 分母不为0 |
| | 连续 $\pm$ 间断 | **一定间断** | 反证法可证 |
| | 间断 $\div$ 连续 | **一定间断** | 分母不为0 |
| | 连续 $\times$ 间断 | **不确定** | 看连续函数是否为0 |
| | 间断 $\circ$ 间断 | **不确定** | 间断性可能相互抵消 |
| **复合函数** | 内连续, 外连续 | **一定连续** | 基本定理 |
| `f(g(x))` | 内连续, 外间断 | **不确定** | 值域是否**避开**间断点 |
| | 内间断, 外连续 | **不确定** | 外函数能否**粘合**间断 |
| | 内间断, 外间断 | **不确定** | 具体分析 |

### 解题步骤

1.  **定类型**: 判断是 **四则运算** 还是 **复合函数**。
2.  **查性质**: 分析在考察点 `x=a` 处，各组成函数的性质（连续/间断）。
3.  **套法则**: 对于结论为“**一定**”的情况，直接得出答案。
4.  **找特例**: 对于结论为“**不确定**”的情况，思考使其连续的特殊条件（如零点吸收、值域避开、跳跃粘合），并尝试构造反例来排除选项。
