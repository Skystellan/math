# 考研线代核心笔记：分块矩阵

分块矩阵是考研线性代数中的一个高频考点与重要工具。它“化整为零，化繁为简”的思想，是解决高阶矩阵行列式、求逆、求幂等问题的利器。

## 一、什么是分块矩阵？

### 1. 定义
将一个高阶矩阵用若干条纵线和横线分割成若干个小矩阵，每一个小矩阵称为原矩阵的**子块**，以子块为元素的形式上的“矩阵”称为**分块矩阵**。

例如，矩阵 $A$ 可以分块为：

$A=\left(\begin{array}{ccccc}1 & 2 & \vdots & 3 & 4 \\ 5 & 6 & \vdots & 7 & 8 \\ \cdots & \cdots & \cdot & \cdots & \cdots \\ 9 & 0 & \vdots & 1 & 2\end{array}\right)=\left(\begin{array}{ll}A_{11} & A_{12} \\ A_{21} & A_{22}\end{array}\right)$

其中，$A_{11} = \begin{pmatrix} 1 & 2 \\ 5 & 6 \end{pmatrix}$, $A_{12} = \begin{pmatrix} 3 & 4 \\ 7 & 8 \end{pmatrix}$ 等等。

### 2. 为什么使用分块矩阵？
- **简化运算**：当矩阵中含有大量零元素或者有特殊结构的子块时，运算可以大大简化。
- **理论证明**：在证明某些矩阵性质时，分块处理能让思路更清晰。

## 二、分块矩阵的运算

### 1. 加法与数乘
- **条件**：两个矩阵 $A$ 和 $B$ 的**分块方式完全相同**，且对应位置的子块都是**同型矩阵**。
- **法则**：对应位置的子块相加或进行数乘。
$$
A+B = \begin{pmatrix} A_{11}+B_{11} & A_{12}+B_{12} \\ A_{21}+B_{21} & A_{22}+B_{22} \end{pmatrix}, \quad kA = \begin{pmatrix} kA_{11} & kA_{12} \\ kA_{21} & kA_{22} \end{pmatrix}
$$

### 2. 乘法
- **核心思想**：形式上如同普通矩阵乘法，但子块相乘时要满足矩阵乘法条件。
- **条件**：$A$ 的**列分块方式**必须与 $B$ 的**行分块方式**完全相同。
$$
\text{设 } A = \begin{pmatrix} A_{11} & A_{12} \\ A_{21} & A_{22} \end{pmatrix}, \ B = \begin{pmatrix} B_{11} & B_{12} \\ B_{21} & B_{22} \end{pmatrix}
$$
则
$$
AB = \begin{pmatrix}
A_{11}B_{11} + A_{12}B_{21} & A_{11}B_{12} + A_{12}B_{22} \\
A_{21}B_{11} + A_{22}B_{21} & A_{21}B_{12} + A_{22}B_{22}
\end{pmatrix}
$$
**注意**：上式中所有子块间的乘法都必须是可行的（即左矩阵的列数等于右矩阵的行数）。

### 3. 转置
- **法则**：先将每个子块进行转置，再将子块看作元素，对整个分块矩阵进行转置。
$$
A^T =
\begin{pmatrix}
A_{11} & A_{12} \\
A_{21} & A_{22}
\end{pmatrix}^T
=
\begin{pmatrix}
A_{11}^T & A_{21}^T \\
A_{12}^T & A_{22}^T
\end{pmatrix}
$$

## 三、重要的分块矩阵类型与性质

### 1. 分块对角矩阵 (准对角矩阵)

形如 $A = \begin{pmatrix} A_1 & O \\ O & A_2 \end{pmatrix}$ 的矩阵，其中 $A_1, A_2$ 都是方阵。

**核心性质 (考点!)**：
- **行列式**：$|A| = |A_1| \cdot |A_2|$
- **逆矩阵**：若 $A_1, A_2$ 可逆，则 $A$ 可逆，且 $A^{-1} = \begin{pmatrix} A_1^{-1} & O \\ O & A_2^{-1} \end{pmatrix}$
- **矩阵的幂**：$A^k = \begin{pmatrix} A_1^k & O \\ O & A_2^k \end{pmatrix}$

推广到多个对角子块的情况，性质依然成立。

### 2. 分块三角矩阵

形如 $A = \begin{pmatrix} A_{11} & A_{12} \\ O & A_{22} \end{pmatrix}$ (分块上三角) 或 $B = \begin{pmatrix} B_{11} & O \\ B_{21} & B_{22} \end{pmatrix}$ (分块下三角)。其中 $A_{11}, A_{22}, B_{11}, B_{22}$ 均为方阵。

**核心性质 (考点!)**：
- **行列式**：$|A| = |B| = |A_{11}| \cdot |A_{22}|$ (主对角线上子块行列式的乘积)。

## 四、核心考点与公式汇总

### 1. 分块矩阵的行列式

这是**最重要的考点**。

**公式1：分块三角矩阵** (最常用)
$$
\left| \begin{matrix} A & B \\ O & D \end{matrix} \right| = \left| \begin{matrix} A & O \\ C & D \end{matrix} \right| = |A||D|
$$
其中 $A, D$ 必须是方阵。

**公式2：分块反对角矩阵**
$$
\left| \begin{matrix} A & B \\ C & O \end{matrix} \right| = \left| \begin{matrix} O & B \\ C & D \end{matrix} \right| = (-1)^{mn} |B||C|
$$
其中 $A$ 是 $m \times n$ 矩阵, $D$ 是 $n \times m$ 矩阵。考研中 $A,B,C,D$ 通常是同阶方阵，此时 $m=n$，公式简化为 $(-1)^{n^2} |B||C|$。

**公式3：拉普拉斯展开的推论 (舒尔补)** (高分技巧)
如果 $A$ 可逆，则：
$$
\left| \begin{matrix} A & B \\ C & D \end{matrix} \right| = |A| |D - CA^{-1}B|
$$
如果 $D$ 可逆，则：
$$
\left| \begin{matrix} A & B \\ C & D \end{matrix} \right| = |D| |A - BD^{-1}C|
$$
**推导思路**：构造一个分块矩阵去乘以原矩阵，使其变成一个分块三角矩阵。
例如，当 $A$ 可逆时：
$$
\begin{pmatrix} E & O \\ -CA^{-1} & E \end{pmatrix} \begin{pmatrix} A & B \\ C & D \end{pmatrix} = \begin{pmatrix} A & B \\ O & D-CA^{-1}B \end{pmatrix}
$$
两边同时取行列式即可得证。

### 2. 分块矩阵的逆

**情况1：分块对角矩阵** (已讲，必会)
$$
\begin{pmatrix} A & O \\ O & D \end{pmatrix}^{-1} = \begin{pmatrix} A^{-1} & O \\ O & D^{-1} \end{pmatrix}
$$

**情况2：分块三角矩阵** (常考)
设 $A, D$ 可逆，求 $M^{-1}$，其中 $M = \begin{pmatrix} A & B \\ O & D \end{pmatrix}$。
**推导法 (比硬背公式好)**：
设 $M^{-1} = \begin{pmatrix} X_1 & X_2 \\ X_3 & X_4 \end{pmatrix}$。则 $MM^{-1}=E$。
$$
\begin{pmatrix} A & B \\ O & D \end{pmatrix} \begin{pmatrix} X_1 & X_2 \\ X_3 & X_4 \end{pmatrix} = \begin{pmatrix} E & O \\ O & E \end{pmatrix}
$$
展开得到方程组：
1. $AX_1 + BX_3 = E$
2. $AX_2 + BX_4 = O$
3. $DX_3 = O \implies X_3 = O$ (因为 $D$ 可逆)
4. $DX_4 = E \implies X_4 = D^{-1}$

将 $X_3=O$ 代入(1)得 $AX_1=E \implies X_1=A^{-1}$。
将 $X_4=D^{-1}$ 代入(2)得 $AX_2 + BD^{-1}=O \implies X_2 = -A^{-1}BD^{-1}$。

**结论公式**：
$$
\begin{pmatrix} A & B \\ O & D \end{pmatrix}^{-1} = \begin{pmatrix} A^{-1} & -A^{-1}BD^{-1} \\ O & D^{-1} \end{pmatrix}
$$
同理可得：
$$
\begin{pmatrix} A & O \\ C & D \end{pmatrix}^{-1} = \begin{pmatrix} A^{-1} & O \\ -D^{-1}CA^{-1} & D^{-1} \end{pmatrix}
$$

## 五、例题精讲

**例1**：计算行列式 $D = \left| \begin{matrix} 1 & 2 & 0 & 0 \\ 3 & 4 & 0 & 0 \\ 1 & 3 & 5 & 7 \\ 2 & 4 & 6 & 8 \end{matrix} \right|$。

**解**：这是一个典型的分块下三角矩阵。
令 $A = \begin{pmatrix} 1 & 2 \\ 3 & 4 \end{pmatrix}$, $C = \begin{pmatrix} 1 & 3 \\ 2 & 4 \end{pmatrix}$, $D_1 = \begin{pmatrix} 5 & 7 \\ 6 & 8 \end{pmatrix}$。
则原行列式为 $\left| \begin{matrix} A & O \\ C & D_1 \end{matrix} \right|$。
根据分块三角矩阵行列式公式，有：
$D = |A| \cdot |D_1|$
$|A| = 1 \times 4 - 2 \times 3 = 4 - 6 = -2$
$|D_1| = 5 \times 8 - 7 \times 6 = 40 - 42 = -2$
所以，$D = (-2) \times (-2) = 4$。

**例2**：已知 $A, B$ 均为 $n$ 阶可逆矩阵，求矩阵 $M = \begin{pmatrix} O & A \\ B & O \end{pmatrix}$ 的逆矩阵。

**解**：设 $M^{-1} = \begin{pmatrix} X & Y \\ Z & W \end{pmatrix}$，其中 $X, Y, Z, W$ 均为 $n$ 阶矩阵。
则 $MM^{-1} = E_{2n}$。
$$
\begin{pmatrix} O & A \\ B & O \end{pmatrix} \begin{pmatrix} X & Y \\ Z & W \end{pmatrix} = \begin{pmatrix} E & O \\ O & E \end{pmatrix}
$$
展开得：
1. $AZ = E \implies Z = A^{-1}$
2. $AW = O \implies W = O$ (因为 $A$ 可逆)
3. $BX = O \implies X = O$ (因为 $B$ 可逆)
4. $BY = E \implies Y = B^{-1}$

所以，$M^{-1} = \begin{pmatrix} O & B^{-1} \\ A^{-1} & O \end{pmatrix}$。

## 六、总结

1.  **核心思想**：分块是**工具**，不是目的。看到高阶矩阵，首先思考能否通过分块简化。
2.  **识别结构**：对**分块对角**和**分块三角**结构要极其敏感，这是最直接的考点。
3.  **牢记公式**：必须熟记分块矩阵的行列式和求逆公式，尤其是特殊形式下的公式。
4.  **掌握推导**：理解公式的推导过程（如构造法求逆），在忘记公式时可以自行推导，也能应对更灵活的题目。

多加练习，熟能生巧！