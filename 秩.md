# 考研数学：矩阵的秩（Rank）超全笔记

矩阵的秩是线性代数中的核心概念之一，贯穿于向量组、线性方程组、特征值与二次型等多个章节，是考研数学的绝对重点。

---

## 一、秩的定义与基本概念

### 1. k阶子式
在 `m × n` 矩阵 **A** 中，任取 k 行和 k 列（`k ≤ min(m, n)`），位于这些行列交叉处的 `k²` 个元素按照原来的相对位置构成的 k 阶行列式，称为矩阵 **A** 的一个 **k 阶子式**。

### 2. 矩阵的秩
*   **定义**：矩阵 **A** 中**非零子式的最高阶数**称为矩阵 **A** 的秩，记作 `r(A)` 或 `rank(A)`。
*   **规定**：零矩阵的秩为 0。
*   **推论**：
    *   若矩阵 **A** 中所有 `r+1` 阶子式（如果存在）都为 0，而至少有一个 r 阶子式不为 0，则 `r(A) = r`。
    *   `r(A) ≤ min(m, n)`。
    *   `n` 阶方阵 **A** 可逆（`|A| ≠ 0`）的充要条件是 `r(A) = n`，此时称 **A** 为**满秩矩阵**。
    *   若 `r(A) < n`，则称 `n` 阶方阵 **A** 为**降秩矩阵**。

---

## 二、秩的求法【核心】

最常用、最重要的方法是**初等行变换法**。

**方法**：对矩阵 **A** 进行初等行变换，将其化为**行阶梯形矩阵**。阶梯形矩阵中**非零行的数目**就是矩阵 **A** 的秩。

---

## 三、秩的重要性质与定理

设 **A** 为 `m × n` 矩阵，**B** 为 `n × p` 矩阵。

1.  **基本性质**
    *   `0 ≤ r(A) ≤ min(m, n)`
    *   `r(A) = r(Aᵀ)` (转置不改变秩)
    *   `r(kA) = r(A)` (其中 `k ≠ 0`)

2.  **秩与可逆矩阵**
    *   若 **P**, **Q** 分别为 `m` 阶和 `n` 阶可逆矩阵，则 `r(PA) = r(AQ) = r(PAQ) = r(A)`。
    *   **重要推论**：初等变换不改变矩阵的秩。

3.  **秩的不等式（常考）**
    *   `r(A + B) ≤ r(A) + r(B)`
    *   `r(AB) ≤ min(r(A), r(B))`
    *   **西尔维斯特不等式 (Sylvester's inequality)**: `r(A) + r(B) - n ≤ r(AB)`
    *   `max(r(A), r(B)) ≤ r(A, B) ≤ r(A) + r(B)`，其中 `(A, B)` 是分块矩阵。

4.  **伴随矩阵的秩（常考）**
    设 **A** 为 `n` 阶方阵，`A*` 为其伴随矩阵。
    *   若 `r(A) = n`，则 `|A| ≠ 0`，`A* = |A|A⁻¹`，所以 `r(A*) = n`。
    *   若 `r(A) = n-1`，则 `|A| = 0`，但 **A** 至少有一个 `n-1` 阶子式不为 0，所以 `A*` 中至少有一个元素不为 0。可以证明此时 `r(A*) = 1`。
    *   若 `r(A) < n-1`，则 **A** 的所有 `n-1` 阶子式均为 0，所以 `A*` 的所有元素均为 0，即 `A*` 是零矩阵。此时 `r(A*) = 0`。

---

## 四、秩与向量组

1.  **向量组的秩**：向量组 `α₁, α₂, ..., αs` 的秩，是指该向量组中**极大线性无关组**所含向量的个数。
2.  **矩阵的秩与向量组的秩的关系**：
    *   矩阵 **A** 的秩 `r(A)` 等于其**行向量组的秩**，也等于其**列向量组的秩**。
    *   `m × n` 矩阵 **A** 的 `r(A) = m` ⇔ **A** 的行向量组线性无关。
    *   `m × n` 矩阵 **A** 的 `r(A) = n` ⇔ **A** 的列向量组线性无关。
3.  **应用**：判断 `s` 个 `n` 维向量 `α₁, α₂, ..., αs` 是否线性相关。
    *   构造矩阵 `A = (α₁, α₂, ..., αs)`。
    *   求 `r(A)`。
    *   若 `r(A) = s`，则向量组线性无关。
    *   若 `r(A) < s`，则向量组线性相关。

---

## 五、秩与线性方程组

这是秩最重要的应用，是每年必考的结合点。

### 1. 齐次线性方程组 `Ax = 0`
（**A** 是 `m × n` 矩阵）

*   **有解性**：`Ax = 0` 必有零解 `x = 0`。
*   **有非零解的条件**：`Ax = 0` 有非零解 ⇔ `r(A) < n` (n 是未知数个数)。
*   **解的结构**：若 `r(A) = r < n`，则其基础解系包含 `n - r` 个线性无关的解向量。通解可表示为 `k₁ξ₁ + k₂ξ₂ + ... + k_{n-r}ξ_{n-r}`。

### 2. 非齐次线性方程组 `Ax = b`
（**A** 是 `m × n` 矩阵，`Ā = (A|b)` 是增广矩阵）

*   **有解的充要条件**：`r(A) = r(Ā)`。
    *   若 `r(A) ≠ r(Ā)`，则方程组无解。
*   **解的唯一性**：
    *   若 `r(A) = r(Ā) = n` (n 是未知数个数)，则方程组有**唯一解**。
    *   若 `r(A) = r(Ā) < n`，则方程组有**无穷多解**。
*   **解的结构**：若 `r(A) = r(Ā) = r < n`，其通解为 `x = η* + (k₁ξ₁ + ... + k_{n-r}ξ_{n-r})`，即 `一个特解 + 对应的齐次方程组的通解`。

---

## 六、秩与二次型

1.  **二次型的秩**：二次型 `f(x₁, ..., xn) = xᵀAx` 的秩，定义为其矩阵 **A** 的秩 `r(A)`。
2.  **秩的不变性**：经过可逆线性变换 `x = Cy` (`|C| ≠ 0`)，二次型的秩不变。
3.  **化标准型**：任何二次型都可以通过正交变换化为标准型 `f = λ₁y₁² + λ₂y₂² + ... + λnyₙ²`。标准型中**非零系数的个数**就等于二次型的秩，即矩阵 **A** 的秩。

---

## 考研总结

1.  **计算是基础**：必须熟练掌握使用初等行变换求矩阵秩的方法。
2.  **性质是关键**：牢记秩的各种性质，尤其是不等式和伴随矩阵的秩，常用于选择题和填空题。
3.  **应用是核心**：秩理论与线性方程组解的判定的结合是整个线性代数的枢纽，是大题的绝对热点。务必掌握 `r(A)` 与 `r(A|b)` 以及未知数个数 `n` 之间的关系。
4.  **联系是升华**：能够将秩的概念灵活运用于向量组的线性相关性判断和二次型中，形成知识网络。